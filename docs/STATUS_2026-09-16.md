# STATUS 2026-09-16 — START HERE

Two things happened today and the second one matters more.

1. **The rest arm was BUILT OUT** — the docked frozen decoder (`15f`), its confusion matrices, and
   the shared-position projection (`15s`). Both are on the share and in the nightly.
2. **A PIPELINE AUDIT found two silent defects across seven rest analyses**, both spread by
   templating, both producing plausible numbers rather than errors. Everything was re-run.
   **Three of the four headline rest results survived; one did not.**

`docs/REST_ENGAGEMENT_AUDIT.md` is the full record of (2) and is the first thing to read before
quoting any rest number. `docs/STATUS_2026-09-13.md` remains the long-form record of the
`restdock05` migration itself (findings 1–11, the three references, the composition tests).

---

## 1. WHAT IS TRUE NOW

| | state |
|---|---|
| rest definition | `restdock05` — docked, `lick_buffer_s [0.5, 1.0]`, `SCHEMA_VERSION 5`, `CACHE_VERSION 12` |
| masks / events | 94/94 + 6 deck-only, 111/111 at v5, verified from each file's own `params` |
| **engagement gate** | **applied in all 7 exposed rest analyses**, guarded by an enumerated test list |
| **classifier** | **repaired (`classify_cues_with_backup`) in all 7** — the raw one collapsed four PRE-STROKE sessions 6→4 positions |
| rest figures | `epoch_15f`, `epoch_15s`, `epoch_15x` all on the share beside the other epoch figures |
| nightly | `15x`, `15f`, `15s` all wired in |
| tests | **1,839 pass** |

### The four headline rest results, re-run gated + repaired

| | published | re-run | verdict |
|---|---|---|---|
| finding 11 (rest carries position) | 1.622, 44/44 | **1.634, 43/44** | SURVIVES |
| persistence at block boundaries | +0.0754, 4/4 | **+0.0772, 4/4** | SURVIVES |
| per-session decode | 93/94 | **94/96** | SURVIVES |
| **15f acute retained** | 0.401/0.187/0.134/0.120 | **0.609/0.277/0.086/−0.093** | **WEAKENED** |

Only 15f's acute cell moved materially, and that is UNDERPOWERING, not error: acute is 16 sessions
and **PS95 contributes ONE**, so its cell moves freely under any reweighting. **Quote 15f's CHRONIC
result** (0.611 frozen vs 1.181 refit, stable across every control); treat acute as underpowered.

---

## 2. THE SCIENCE, AT ITS ACTUAL STRENGTH

**REST CARRIES POSITION** — 1.634 observed/null, 43/44 sessions, 4/4 animals, and 94/96 sessions
decode above their own circular-shift null. This is the solid one.

**THE REST CODE IS PARTLY REPLACED, NOT RECOVERED** (`15f`). Chronically the within-session refit
reaches 1.181 of its own pre-stroke level while the frozen pre-stroke model reaches 0.611: position
is in chronic rest, read by something other than the pre-stroke code. The refit arm is paired on the
SAME periods, so the only difference between the arms is the estimator.

**THE REST POSITION CODE IS NOT THE TASK POSITION CODE.** `15s` finds the task map and that
position's own rest map close to unrelated — cosine **0.09 post-cue, 0.16 pre-cue**. Both facts hold
together: rest carries position, in a largely DIFFERENT spatial code. Consistent with 15f's
confusions, where far-contra REST holds up (0.50 → 0.38 → 0.39) while far-contra TASK collapses
(1.46 → 0.47).

**THE PRE-CUE SIGNAL CARRIES A BACKWARD-POINTING TRACE.** `15s`: the pre-cue window shares
position-specific structure with rest (+0.060 pre, +0.188 acute, 4/4 animals) and the post-cue
window does not (flat against its null at every epoch). The block-boundary test says which way that
shared component points — rest resembles **the position just licked at**, not the one coming next
(+0.0772, 4/4). So the pre-cue signal carries a trace of the LAST target rather than preparation for
the next: **a real constraint on reading it as anticipatory**, and consistent with this project's
standing refusal to call it "a maintained motor plan".

**ACUTE ERRORS COLLAPSE, THEY DO NOT SCATTER** (15f confusions). Acute predictions pile onto
near-contra and far-row errors shift toward near columns (0.56 → 0.67). The rest code SHIFTS rather
than simply degrading.

### Read these with the caveat attached
* **`15s` IS SMALL.** ~0.06 null-corrected, cosine 0.16. It is a CONSTRAINT, not a result, and it
  was overstated twice before landing there (first raw values the null eats, then a 0.065 residual
  described as "a component of the pre-cue signal").
* **`15s` SUBACUTE IS PS92 ALONE** (−0.639 against +0.212, +0.044, +0.027). Exclude subacute.
* **15f ACUTE IS UNDERPOWERED** — see above.
* **PS92 IS THE SNR FLOOR** — the six lowest rest fractions in the cohort are all PS92.

---

## 3. WHAT THE AUDIT FOUND (full record: `docs/REST_ENGAGEMENT_AUDIT.md`)

**DEFECT 1 — a documented gate that was never implemented.** Four rest analyses stated a
working-trials gate in their docstrings; `rest_position_decode` printed "(working trials…)" in its
own stdout header. The only filter applied was that the bracketing trials agree on POSITION. The
quit period is **3.1% of rest frames pre-stroke and 18.7% acute**, so the contaminant tracked the
independent variable.

**DEFECT 2 — the raw classifier.** PS92/PS93/PS94/PS95_0806 — one PRE-STROKE session per animal —
collapse from six positions to four under `_classify_cues`, 144–192 trials mislabelled each.
**Fourth recorded occurrence** of the defect pitfall 6 already names.

**DEFECT 3, introduced WHILE FIXING the other two** — a patch snippet referencing `a` as an argparse
namespace where it was the rest-bout loop variable. Caught by `ruff F821` before it ran.

All three spread the same way: scripts written from each other as templates, copying prose and
structure together.

**CORRECTLY UNGATED, LEFT ALONE:** the mask builders (the animal IS at rest; gating there would
redefine the mask and force the whole recompute + schema + cache cascade for what is an inclusion
criterion), quiet-vs-running in preprocessing (**the imaging box needed no re-run**), and
`locomotor_state`, where rest is a CLASS being decoded rather than a baseline.

**ALREADY CLEAN:** `_RESTWref_` and the encoder's `_restw_baseline` — gated since 2026-09-14 via
`rest_frames_by_position(engaged_only=True)`. An earlier draft of the audit claimed otherwise; that
was a file-level check too coarse to see one gated builder and one ungated one in the same module.

**`_quiet_zscore` RESOLVED, no action** — SD unchanged (1.0000), mean shifts 0.033 σ in
acute/subacute only, absorbed by every consumer, and the frozen decoder never reads that path.

---

## 4. PENDING

**Re-runs the fixes make necessary but that have NOT been run:**
* `flatpool_vs_restw` — gated now (it reads through `session_rest_svt_timelocal`), not re-run. The
  `restw` composition decision rests on it.
* `rest_carries_position` — gated now, not re-run.

**Not built:**
* **The delta-rest figure** — `pooled("flatpool",e,q) − pooled("restw",e,q)`, NOT
  production-`rest` minus `restw` (that conflates composition with temporal). Prediction to test:
  the delta should GROW post-stroke.

**Not pulled** (bounded reading, not re-runs — all have `.csv`/`_sessions.csv`/`_meta.csv`):
* encoder R²/FEVE, RSA/crossnobis (105 `crossnob*` CSVs), `epoch_10_best_match_acc_*`.

**`docs/PRELIM_DATA_VLS_STROKE.md` STILL CARRIES PRE-AUDIT REST NUMBERS.** Its 15f and 15s sections
were written from the ungated runs and from the first (doubly-docked) 15s. **Update before any of it
reaches a slide.**

**A PROVENANCE HAZARD ON THE SHARE, not yet resolved.** Two 15f figures sit side by side in
`grant_figures/epoch/`:

* `epoch_15f_rest_frozen_restdock05_durmatched_gapmatched.png` (18:13) — **UNGATED, superseded**
* `epoch_15f_rest_frozen_restdock05_gated.png` (18:34) — gated, current

Nothing on either panel says which is which, and the superseded one has the more descriptive name.
Rule 1 forbids deleting on MICROSCOPE, so the fix is to **re-run 15f with no `--tag`** — the gate is
now the default, so the same flags regenerate the `_durmatched_gapmatched` stem with gated content
and the ambiguity disappears without a delete. ~11 min. **Do this before anyone reads either
figure.**

**Open science questions:**
* **SUBACUTE DOES NOT REPLICATE** — between-animal r 0.030/0.052 against acute 0.810. Untouched by
  today's work.
* **Settling transient** unresolved: median 2 s, p90 29.4 s, tail to 302 s.

**Deck / housekeeping:**
* `coding_rtdrift.png` is 08-22 and still placed but NOT in the manifest `figures` map.
* `epoch_14_beta_maps_MEANref_lick_lick_stats` has no Far Contra / acute − pre row.
* 9/15 sessions are registered and masked but NOT in the deck (its date list was fixed at launch).
* `15x`/`15f`/`15s` builders still live in `scripts/rest_migration/` — roadmap #3 fold-in not done.
* sklearn `multi_class` is REMOVED in 1.8; will break, not just warn.
* **Machine split:** dev here, nightlies on the other box. It must be on current `main` before it
  runs a rest analysis, or it will produce ungated numbers under the same filenames.

---

## 5. PITFALLS FROM TODAY

1. **A DOCUMENTED CONTROL IS NOT AN APPLIED CONTROL.** Four analyses claimed a gate none ran, one
   printing it in its own output. **Assert controls in CODE.**
   `tests/test_rest_engagement.py::EXPOSED` now enumerates all seven exposed analyses and checks
   each builds the gate AND uses the repaired classifier.
2. **TEMPLATING SPREADS DEFECTS WITH STRUCTURE.** All three defects propagated by writing scripts
   from siblings. When you copy a collection loop, copy its GUARDS or delete its claims.
3. **A PARTIAL COHORT IS NOT A TREND.** A 4-of-44 smoke gave 1.469 and was read as "the direction is
   down". The cohort came in at 1.634 — UP. Same trap `CLAUDE.md` documents for render timings,
   recurring in a statistic.
4. **"PATCHED" MEANS VERIFIED, NOT REPORTED.** `rest_block_boundary` and `rest_carries_position`
   were described as fixed for both defects when they had only the classifier fix — the patch script
   had reported success for a different change. **Count call sites.**
5. **A SIGN CONVENTION RESTATED IS A SIGN CONVENTION GUESSED.** The frozen-vs-refit gap was written
   up as "frozen minus refit" and every post-stroke cell read backwards. Take a sign from the
   `_meta.csv` `ylabel` or the code, never from memory.
6. **CHECK FOR AN EXISTING CONTROL FAMILY BEFORE DEFERRING A RESULT.** The task arm's training-set
   matched `5rm` already existed when the frozen-vs-refit result was deferred over "data poverty" —
   an objection that also pointed the wrong way.
7. **A COMPARISON WHOSE TWO HALVES ARE IDENTICAL ANSWERS INSTANTLY AND CONVINCINGLY.** The MEANref
   balance test compared `balance=None` against `balance=True` on the `lick` arm, where None
   resolves to True, and printed a column of +0.000 deltas.
8. **MEASURE THE INPUT, NOT EVERY CONSUMER.** `_quiet_zscore` was settled for all three of its
   consumers in one pass by measuring how much the statistic it computes actually moves.
9. **`git commit -m` breaks; use `-F`.** **`| head -N` kills the producer (SIGPIPE).**
   **Scripts run by absolute path need `PYTHONPATH=.`** — the editable-install finder does not see
   newly added modules.
