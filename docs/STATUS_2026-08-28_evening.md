# Handoff, 2026-08-28 evening — what is done, what is running, what is next

## Running right now

| | |
|---|---|
| **grant render** | PID 3568, started 09:48. Slow (see DECISIONS: `_pooled_bundle` never adopted). Producing figures — the other window's layout reporter has flagged `grant_8b_crossnobis_geometry_cue_lick.png`. **Do not restart it.** |
| **watch** | A background task fires when PID 3568 exits, then reports the `grant_*` count. On that signal: rebuild the deck and publish. |

## Done today (all pushed)

`e7d1935` basis server fallback · `74b65aa` PS95_0813 misattribution · `09417f7` feature disk cache ·
`c84250f` frozen models as objects · `af422cf` ood_control pre-only · `641118f` 8/17 pooling ·
`fd67f63` no-lick reference · deck consolidation (`2490eb6` `21f2333` `121ffae` `ac9a5d5` `b2c705e`) ·
layout (`139748c` `1b1a8f7` `541a160`)

Then the five approved items and what followed:

| commit | |
|---|---|
| `837a8e3` | **Early vs late rewarded** — G9e, split at fixed 2.0 s, `CONFUSION_SUBCLASSES` |
| `73b651d` | **Frozen decoder: six recipes to one** — `frozen_decoder_models()` |
| `b93fbc0` | **Engaged cut from config** — AST guard found a FOURTH module (`postcue_window_test`) |
| `c63ff54` | **Lick discriminator: three copies to one** — number-preserving |
| `f3601d6` | **14 unshown figures placed** — 496 → 525 slides |
| `1084b36` | `figure_cross` drew into row 0 of a two-row grid |
| `2e33234` | **The late arm is 3.4%** — the measurement refuted the premise; claims corrected |
| `557ea72` | Root env override moved into `PathResolver` (third-machine readiness) |
| `43bede0` `ace8c18` `f248ab9` | **Epochs** — assignment, pooling, session dots |

**Published to MICROSCOPE:** 539 PNGs + 7 JSONs, `frozen_conflicts: []`. Server now carries all 12
G9e, 21 small-lesion, 3 basis-health, 44 RSA-hemisphere figures. **The deck was NOT published** — its
section H would regress against the copy already there until the render lands.

## Pending, in the order I would do them

1. ~~**Migrate four sites to `_pooled_bundle`**~~ — **DONE.** `pool_sessions` is now called from
   exactly one place in the module, pinned by a test. Safe to do during the render after all: the
   run is a single `-m wfield_local.grant_figures` process with no children, so it holds the module
   in memory and an edit on disk cannot reach it; the tree was clean, so no uncommitted work was at
   risk. **The running render does NOT benefit — it is still executing the old code.**
2. **Absolute inch margins for `_delta_grid`** — the 11th post-stroke day crowding. Their file.
3. **Rebuild deck + publish** on the watch's signal. Deck is currently 525 slides locally.
4. **Extract per-session collectors for figs 8g, 10, 10b, 11.** They build per-day data INLINE
   rather than through a `_collect_*`; `epoch_figures` must not re-derive it, or a second definition
   of the same population enters the codebase.
5. **Write the epoch renderers**, then **drive each through `_overlaps` at 6.2in**. The driving is
   not optional: larger fonts on a smaller canvas is exactly where labels collide, and three layout
   faults this month were found only by driving rather than reading the diff.
6. **Deck section for the epoch figures**, placed 4-up at quarter page.

7. **Parallelise the render.** Measured 2026-08-28: 1.51 of 24 cores over 5.65 h, 6.3% of the box,
   with 42 GB free. The bigger win than anything left in the serial code, and the real answer to
   "how do we make the nightly faster". **Parallelise by figure FAMILY or by (animal, alignment),
   not by figure** — `_BUNDLE_CACHE` and the collector `lru_cache`s are per process, so one figure
   per worker re-pays every shared collection and raises total CPU. See DECISIONS for the full set
   of caveats (Agg, processes not threads, `session_cache` write races, `OMP_NUM_THREADS`).

Full scope and reuse map: [`docs/EPOCH_FIGURES_PLAN.md`](EPOCH_FIGURES_PLAN.md).

## Still open from earlier

- **No-lick**: regenerated by the other window at 10:10 (post-`c9e8142`), so it is CURRENT. My queued
  re-run was redundant and was killed. `nolick_reference_prestroke.json` is deliberately untouched —
  both fixes are no-ops on a pre-stroke-only pool, and `JSON_FROZEN` refuses to overwrite it.
- **`frozen_models` on the server holds PS92 only** (decoder + encoder, cue). Everything else refits
  as `frozen-new` — correct and deterministic, just not free.
- **PS95 rolling decoder 23.7 min**; PS95 is the only animal with more `stopped` (942) than
  `miss_working` (611).

## Third-machine readiness (audited)

**Present:** all 74 pooled sessions have `SVTcorr.npy`, `allen_aligned_*/U_atlas.npy` and LocaNMF
outputs on MICROSCOPE — so a new box need not run LocaNMF for existing sessions. Joint bases for all
four animals are on the server. Repo is config-driven with no machine-specific paths.

**Gaps:** `WIDEFIELD_MACHINE` and `WIDEFIELD_FIGURES_WORKING` must be set explicitly (`detect_machine`
knows three profiles and falls back to `analysis`); the deck's `--src` defaults to the LOCAL figure
dir and must be pointed at `cue_analysis_out`; **the GPU stack is not pip-resolvable** (torch CUDA,
`wfield==0.6.0`, LocaNMF/localnmf, cuhals — custom Windows build, patches in `wfield_local/*.patch`)
and is the real blocker for RUNNING LocaNMF; and a cold `session_cache` means the full ~9.6 h stage.

## Method notes that cost time this session

- **Verify a layout fix by DRIVING the function.** Three faults this month: a comment asserting an
  `hspace` the gridspec never got; a `fig_asymmetry` fix applied to `fig_delta_trajectory`; and
  `figure_cross` indexing `axes[0][k]` after a reshape to two rows.
- **A missing figure is an ABSENCE, not an error.** `_draw` catches plotting bugs so a run survives
  them, and the deck's `_f.exists()` skip fires before the manifest records anything — so a broken
  figure reports as **0 missing**. Only instrumenting `add_picture` on a real build finds it.
- **Logged fault counts are floors** — `_save` prints `bad[:6]`.
- **Heredocs through Git Bash halve `\n` escapes.** Two syntax errors this session. Write patch
  scripts to a file instead.
- **`git add -A` on this shared checkout sweeps the other window's work.** Stage explicit paths.
