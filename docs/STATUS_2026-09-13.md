# Status 2026-09-13 — the REST baseline migration

**Branch `worktree-rest-baseline`** (pushed). Non-quiet findings were cherry-picked to `main` as
`70aed76`; everything else lives on the branch. `main` also carries the other window's work, which
this branch is rebased onto (`4006df3`).

---

## THE ONE-LINE STATE

The quiet baseline was redefined and the cohort rebuilt; the headline anatomical result survived it
unchanged; and a control Priya asked for exposed a **real remaining defect — the map reference
subtracts a FLAT session mean where it should subtract a TIME-LOCAL one.** The fix is implemented
and **NOT yet wired**, so every REST-referenced figure on the share is one pass out of date.

---

## WHAT CHANGED, AND WHY

### The REST baseline replaces "quiet"

`segmentation.quiet` excluded **8 s after every reward** — carried over from stroke_orofacial, whose
task runs on an 8 s post-tone window; ours has a 3.5 s response window. Worse, anchoring on REWARD
anchors on PERFORMANCE, so the category tracked the deficit.

**REST = between trials AND not running AND not licking.** Window: `cue + response_window + 0.5 s`
→ next **`trial_start`**.

| | retired | REST |
|---|---|---|
| acute/pre rest fraction | **3.89** | **1.11** |
| chronic median rest | 0.007 | **0.030** |
| rest bouts, cohort | 14,017 | **26,010** |
| bouts admitting a 1 s segment | 0.58 | **0.846** |
| bouts admitting 2 s | 0.17 | 0.194 |

Every parameter carries its measurement in `configs/defaults.yaml segmentation.rest`. The four
scripts that produced them are in `scripts/rest_migration/` with a README.

Key sub-findings:
* **`trial_start`, not the strobe** — firmware moves the spout and only THEN pulses the strobe, so
  the strobe fires AFTER the movement. `trial_start` precedes it on 16,607/16,607 trials by a median
  0.925 s, which IS the travel time.
* **Settle 0.5 s** — reward lands 6 ms after the cue, so the trial window already contains it; the
  settle is pure margin. 0.5 s leaves 2.02 s of rest per trial against 1.52 s at 1.0 s.
* **No reward term at all.**
* **The state decoder's 1 s window STANDS** — re-derived, not inherited: 2 s fits 19.4% of the new
  rest bouts against the 17% that rejected it originally, while 1 s fits 84.6% against 58%.
* **Chronic is still 0.51 of pre** and that is the LICK term (73.7% of samples excluded pre-stroke,
  82.5% chronic). Behaviour, not a definition artefact. The chronic caveat stays.

### Naming

"quiet" is retired as a word. `rest` / `_RESTref_` / `quiet_<tag>_rest/`; the old definition is named
**`reward8`**. **The filename carries the baseline** (`reference_tag`), so a figure built on the old
masks is called `_REWARD8ref_` and cannot be mistaken. Old masks are never deleted; retiring is one
config line (`segmentation.rest.variant`).

---

## FINDINGS (all documented in DECISIONS.md, 2026-09-12 / 09-13)

### 1. The far-contralateral acute result survives everything

Cross-position null — each animal's own `epoch − pre` map, mean pairwise r between animals, against a
null that re-pairs animals **across positions** (preserving anatomy/warp/rim/glue, destroying only
position-specific agreement):

| Far Contra, acute − pre | observed | null | p |
|---|---|---|---|
| MEAN | +0.819 | +0.018 | 0.001 |
| REST | +0.812 | +0.071 | 0.001 |
| PRECUE | +0.856 | +0.083 | <0.0005 |

Unchanged from the retired baseline's +0.769. **A null is mandatory here**: the PRE-STROKE maps
already correlate between animals at r ≈ 0.88, so a large r is this statistic's resting state.

### 2. MEAN's near-position "increases" are the coupling artefact, measured

Near Middle acute: **+0.655, p = 0.003 under MEAN**, absent under both uncoupled references
(p = 0.243, p = 0.190). Under the MEAN reference a high between-animal r is what coupling PREDICTS.

### 3. The chronic column is uninterpretable under EVERY reference

REST chronic: observed +0.43 to +0.72 against nulls of +0.50 to +0.51, every p > 0.2 — unchanged
despite estimating chronic from 4.3× more frames. **More frames did not fix it.**

### 4. A position-graded amplitude change, NOT yet established

Acute amplitude vs pre, REST: **1.48 / 1.21 / 1.27 (near) → 1.01 / 0.69 / 0.48 (far)** — monotone
across six independently-analysed positions. Only far-contra has a position-specific SHAPE.
**Nothing has tested the gradient itself**; the test that would is per-animal amplitude ratios plus
the nested bootstrap on the near-minus-far contrast.

### 5. REST "carries position information" — it does not; it is DRIFT

6/6 positions significant (up to 1,042/2,022 bins), ratio 1.45. But positions run in ~6-trial
BLOCKS, so position is confounded with time-in-session:

    DRIFT     same position, early vs late      RMS 0.00282
    POSITION  different positions, matched time RMS 0.00288    **ratio 1.02**

Drift aliased onto blocks, not position coding. **The reference is not conceptually broken** — but
it exposes the defect below. Figure: `epoch_15x_REST_by_position_by_animal.png`.

### 6. THE REMAINING DEFECT — flat vs time-local subtrahend

`position_reference_maps.session_quiet_svt` subtracts ONE SESSION MEAN, which is flat and cannot
remove drift. `locanmf_position_encoder._quiet_baseline` has ALWAYS used a time-local baseline.
Same quantity, two treatments; the encoder's is correct.
`session_rest_svt_timelocal` now implements it map-side (12 bins, median, interpolated; carries
SD 0.047 of across-time structure the flat mean is blind to). **NOT WIRED** — see TO-DO.

### 7. The pre-cue reference was SELF-SUBTRACTING on the pre-cue arm

Feature window `[cue−2 s, cue]`, baseline `[cue−1 s, cue]` — the baseline was the window's own second
half. Skipped now; 13 files moved to `retired_INVALID_PRECUEref_on_precue_arm/`.

### 8. Pre-cue window evidence is variant-mismatched

Three variants across the evidence and **none is production**: the withdrawn sweep (`zerophase`/ROI),
the 08-13 re-run (`strobedetrend`/ROI/16 sessions), and what we run (`meegkit_hpfit`/LocaNMF). The
08-13 re-run found information spread EVENLY and kept `precue_post_s: 2.0`. **A second reading is
open**: the early ENL begins at the last lick violation, so it carries lick bleed-through that a
decoding score cannot distinguish from position code. The 2 s ENDING at the cue is the safest
interval. Sweep running (see below).

---

## RUNNING RIGHT NOW

| job | state |
|---|---|
| pre-cue window sweep, 4 arms (`mean1.0`, `mean2.0`, `roll4x0.25`, `roll4x0.5`) | ~8/44 sessions |
| lick-aligned REST-normalised maps | ~3/92 |
| `nightly_figs all --skip-grant` | in the encoder figures |

Early sweep read (ONE session, direction only): `mean2.0` 0.405 > `roll4x0.5` 0.350 > `mean1.0` 0.341
> `roll4x0.25` 0.273. If it holds: maps stay at 2 s, and `decode.bins.precue: 4` may not be earning
its keep either. **Check trial counts per arm** — the 1 s arms retain more trials (easier lick-free
placement), so the arms are not scored on identical sets.

---

## TO-DO

1. **WIRE THE TIME-LOCAL BASELINE** and redo every REST-referenced map. `session_raw_maps` averages a
   position's trials and subtracts one map; the time-local version must be evaluated at THOSE
   TRIALS' times, which needs per-trial frame indices that function does not currently carry.
2. **Re-render `12b`/`13` (state decoder) cleanly.** Tonight's render STRADDLED the events rebuild —
   the precue arm (9.91 h) predates it, the cue arm (8.72 h) follows it, so the arms are on different
   rest definitions with nothing on the figures saying so.
3. **The state decoder's own drift confound (NEW, no fix applied).** Rest is a CLASS there, not a
   subtrahend, so a time-local baseline does not reach it. Rest segments early vs late differ by
   RMS 0.00282 for no behavioural reason, and classes are not distributed alike across session time
   (licking concentrates on trials, rest on ITIs) — so the decoder can separate classes partly on
   WHEN rather than on behaviour. Priya, 2026-09-13: do the simple version for now; only balance
   across time bins if there is enough of each class per bin. **First step is that feasibility
   check** — segments per class per time bin — which is cheap and not yet run.
4. **Second deck rebuild** once the lick maps land; the decks currently MIX baselines
   (`quiet_running` new, `lick_quietnorm` was 512 h old).
5. **`cue_vs_lick` maps** (519 h old) consume the lick maps — likely stale, unchecked.
6. **Verify the encoder's 0.749 → 0.286 → 0.745** against the new masks.
7. **Test the amplitude gradient** (finding 4), and check whether block ORDER is systematic across
   sessions before leaning on it.
8. **Deck slide edits** Priya listed 2026-09-13: delete FEVE pooled (23), blank (57), D2 ROI (80–82,
   85), G2 (102–103), small-lesion G2d (108–113), G7b/d (121–123), H5/H5b (141–144), H6 (155–159,
   collapses pre-stroke), H8d/H8e, H10, H10b. Modify: G4/G4b/G6 (illegible), G5 (legend over data),
   H8 (drop cell numbers). Move H7d after H6d. **KEEP 27–35** (pre-cue no-lick, F13's support).

---

## RECURRENT ISSUES — all of these bit tonight

1. **ERRORS THAT REPORT SUCCESS.** Four times: `_REF_TEXT` KeyError silently dropped a third of the
   15r figures while the render printed `96 units wrote, 0 failed`; the self-subtracting precue map
   rendered cleanly; the mask naming mismatch broke nothing until `preprocess` would have needed it;
   and a control printed "no significant position dependence" while testing ZERO positions.
   **Three of four were caught by COUNTING or COMPARING OUTPUTS, not by an error.** Keeping the
   retired figures instead of deleting them is what made two of them catchable.
   → **Count what a render produced against what it was asked for. Exit status is not evidence.**
2. **A CONTROL THAT CANNOT FAIL IS WORSE THAN ONE THAT FAILS.** The vacuous verdict came from an
   intersection that emptied silently. → Print the INPUT to a test (n per cell) before its output,
   and make "nothing was tested" a distinct verdict from "nothing was found".
3. **THE WORKTREE `sys.path` TRAP.** `python scripts/x/y.py` puts the SCRIPT's directory on
   `sys.path[0]`, so `import wfield_local` resolves through the editable install to the **main
   checkout**; `python -m scripts.x.y` imports the worktree. They read different configs and produce
   different definitions with no error. → **Always `python -m` from a worktree.**
   `recompute_masks._assert_package_matches_cwd` enforces it for anything that writes.
4. **CACHES KEYED ON A NAME WHOSE MEANING CHANGED.** `quiet_starts` kept its name at events v3.
   `get_or_compute` would have served the retired definition forever. → Bump the schema whenever
   MEANING changes, not only when shape does. And note `nightly_figs` NEVER regenerates the events
   npz, so the bump alone is not enough — `scripts/rest_migration/regen_events.py` does it.
5. **BACKTICKS AND `\n` IN BASH.** Backticks in a `git commit -m` triggered command substitution and
   mangled three lines; heredocs turned `\n` into real newlines and broke string literals twice.
   → **Write commit messages and generator scripts to a FILE, never inline.**
6. **A SECOND SESSION WORKS IN THIS REPO.** Its commits appear mid-task. → Never `git add -A` without
   reading `git status`; expect `DECISIONS.md` append-append conflicts (keep both).

---

## PERTINENT CONTEXT

* **The position DECODER never reads the rest mask.** `_build_signal` loads footprint-scaled LocaNMF
  `C` or raw `U`/`SVT`. Headline decoding, the frozen decoder, LOSO, figure 14's beta maps, and the
  MEAN and PRECUE references are all untouched by this migration.
* **Three map families decompose**: fig 14 = data + mean reference + decoder/Haufe estimator;
  15r MEANref = data + mean reference; 15r REST/PRECUE = data. fig14-vs-15rMEANref isolates the
  ESTIMATOR; 15rMEANref-vs-REST isolates the REFERENCE. The first CANNOT diagnose coupling — both
  share the reference.
* **Three pre-cue intervals, now named in config**: `precue_post_s` 2.0 (analysis window, F13),
  `precue_baseline_s` 1.0 (what `baseline: precue` subtracts), `precue_lickfree_check_s` 2.0 (QC
  gate). They are not interchangeable and conflating them caused finding 7.
* **Sidecars redraw, they do not re-analyse.** A bundle stores computed cells AND the subtitle, so a
  redraw reproduces stale text faithfully. They cannot serve a re-render.
* **Imaging-box outputs written from here.** `running_activity_*` and `lick_aligned_*` were rebuilt
  from the analysis box into `{mc}/`. That box owns them and will regenerate on its next nightly,
  from the same masks, to the same answer. Convenience, not handover.
