# STATUS 2026-09-17 — START HERE

Supersedes `docs/STATUS_2026-09-16.md` for everything after the rest-arm audit. That file remains
the record of the engagement-gate audit and the `restdock05` migration; this one covers the
**transfer / rotation arm** built on top of it, and **four corrections to things this project
believed** that were found by reading figures rather than code. Three of the four were
corrections to things **I** asserted in conversation; Priya caught every one by asking whether it
matched what the repo already knew. When a claim here disagrees with an older doc, check whether the
older doc predates `restdock05` (2026-09-13) or the `meegkit_hpfit` flip (2026-08-14) before
believing it.

---

## 0. READ THIS FIRST — FOUR CLAIMS THAT WERE WRONG

These were all live in docs or in my own reasoning during 09-17. Each is corrected below with the
measurement that settles it. **Do not re-derive an argument from the superseded side.**

| claim, as previously stated | status |
|---|---|
| "the 0.1 Hz zero-phase filter smears post-cue backwards, inflating pre-cue decoding by ~0.21" | **applies to the RETIRED `zerophase` product only.** The adopted variant is `meegkit_hpfit` and it does NOT smear |
| "`PREPROCESSING_DECISION.md`: nothing downstream has been re-run" | **STALE.** The re-run happened; `config.hemo_variant()` returns `meegkit_hpfit` and `svtcorr_path()` routes every analysis to it |
| "QUIETref is the endorsed deficit reference" | **QUIET IS RETIRED.** It is `_RESTref_` now; `retired_QUIETref/` holds the old set |
| "the quiet baseline drifts +0.0026 against a ~0.005 signal" (~52% of signal) | **QUIET-era, and SUPERSEDED.** `uniform_shift_check` measured the restdock05 baseline at a median **3.1%** of the evoked signal |

### 0a. THE FILTER DOES NOT SMEAR

`config.hemo_variant()` -> **`meegkit_hpfit`**, and `config.svtcorr_path()` sends every analysis to
`hemo_meegkit_hpfit/SVTcorr.npy`. The bare `SVTcorr.npy` beside it IS the zerophase product and is
no longer read. That function exists precisely because five modules had the zerophase path
hardcoded and "would have gone on silently reading the superseded data -- no error, no missing
file, just the old result."

`meegkit_hpfit` (order 10) fits drift on a mask **excluding strobe−0.25 s → cue+4 s**, so the fit
never sees the measured window and nothing is smeared in time. Adopted 2026-08-13, flipped
2026-08-14 (`joint_locanmf` records it). Pre-cue 0.352, post-cue **0.759** — the best post-cue of
anything tested, +0.075 over zerophase.

**`docs/PREPROCESSING_DECISION.md` line 3-5 still says the re-run is pending. It is not. Fix that
line.**

### 0b. QUIET vs REST vs restdock05 vs restw — THEY ARE NOT SYNONYMS

`wfield_local/quiet_periods.py` opens: *"REST-period detection ... the file still named for its old
category."*

| term | definition | status |
|---|---|---|
| **QUIET** | slow-treadmill AND not-near-lick AND **NOT-NEAR-REWARD (8 s)** | **RETIRED.** Anchored on the animal's PERFORMANCE, so the category tracked the deficit: **4.4% of frames pre-stroke, 17.1% acutely, 0.7% chronically** |
| **REST** | outside every trial AND slow treadmill (buffered) AND away from licking (buffered) | current. Anchored on the TRIAL, which happens whether or not the animal succeeds |
| **restdock05** | the current REST **variant** — docked, `lick_buffer_s [0.5, 1.0]`, settle 0.5 s | current |
| **rest** (reference) | the **time-local** rest baseline, subtracted from SVT at each trial's own time | one of `REFERENCES` |
| **restw** (reference) | the **position-weighted FLAT** baseline — six per-position medians averaged equally | one of `REFERENCES` |

`quiet_periods.py`, `quiet_variant()`, `quiet_starts` in the event npz are all LEGACY NAMES FOR
REST. The parked item "quiet->rest wording in ~29 slide strings" is this.

**Consequence, and the correction matters.** The drift figures (pre −0.006871 ... chronic
−0.004285, "+0.0026 against a ~0.005 signal" — about HALF the signal) are from `STATUS_2026-09-12`
and describe the **QUIET-era** baseline. They were used on 09-17 to argue `_RESTref_` cannot carry
an across-epoch amplitude claim. **That argument does not survive.**

`scripts/rest_migration/uniform_shift_check.py` already measured the restdock05 baseline, before the
config change (DECISIONS.md, 2026-09-13):

> the baseline moves by a median **3.1%** of the evoked signal (p90 0.091, max 0.155), and
> `cos(d, evoked)` is **negative in 12/19** — mixed, not the shared direction contamination would
> produce. The large shifts are the sessions with the large frame gains, i.e. thin baselines being
> estimated better, not lick activity leaking in.

**3.1%, not ~52%.** The rest baseline under `restdock05` is not a serious confound for across-epoch
amplitude, and `_RESTref_` is usable for framing (1).

That script exists for exactly the blind spot this arm keeps rediscovering — its docstring: *"position
spread is a DIFFERENTIAL measure by construction ... a shorter buffer that admits lick-adjacent
frames EQUALLY at every position would raise all six baselines by the same amount, leave the spread
untouched, and still shift every `map - rest` amplitude."* That IS the framing (1) vs (2) distinction
in §2.

**SCOPE CAVEAT, so this is not over-claimed the other way.** `uniform_shift_check` measured the shift
caused by RELAXING THE LICK BUFFER (`[1.0, 2.0]` → `[0.5, 1.0]`) over 19 sessions. It bounds the
baseline's sensitivity to a definitional change at ~3%; it is not the same quantity as "how much does
the restdock05 baseline drift ACROSS EPOCHS". The epoch-wise version appears never to have been
measured directly, and is cheap to do.

---

## 1. WHAT WAS BUILT

### `epoch_15g` — the cross-epoch TRANSFER MATRIX (`scripts/rest_migration/transfer_arms.py`)

Train a decoder on epoch A, score it on epoch B, for every ordered pair, in the shared joint
LocaNMF basis. Four windows: **ENL** (precue/working), **cue** (cue/working), **lick** (lick/lick),
**rest** (restdock05). Core in `wfield_local/transfer_matrix.py`, 18 guards.

**THE TOP ROW IS NOT NEW** — `train pre -> test post` is what the existing frozen decoders already
report. Rows 2-4 are new, and `chronic -> pre` is the only cell where ROTATION and ADDITION predict
different things. Differences from the published families in the shared row: training size
block-matched in EVERY cell (as `5rm`, not `5r`), and pooled balanced accuracy rather than
per-position. **The block-permutation null is NOT a difference** — `decode_ci.frozen_ci` already
uses it.

**Result 1 — ADDITION IS REJECTED IN ALL FOUR WINDOWS.** Raw above-chance asymmetry
(`chronic->pre` minus `pre->chronic`), PS92 / PS93 / PS95:

| arm | asymmetry | verdict |
|---|---|---|
| ENL | −0.061 / −0.004 / −0.079 | SPLIT (2/3 material) |
| cue | −0.095 / −0.088 / −0.021 | SYMMETRIC |
| lick | −0.050 / −0.020 / −0.007 | SYMMETRIC |
| rest | +0.002 / +0.021 / +0.015 | SYMMETRIC |

Addition predicts a LARGE POSITIVE asymmetry. None is material in either direction. The transfer is
symmetric — a **partial rotation**, retaining 0.79/0.78 of ceiling for cue and 0.81/0.82 for lick,
0.64/0.57 for ENL, 0.35/0.53 for rest.

**Result 2 — EVERY WINDOW ENDS ABOVE ITS PRE-STROKE CEILING.** Own-epoch above-chance:
ENL 0.29 → 0.25 → 0.29 → **0.33**; cue 0.62 → 0.39 → 0.59 → **0.70**; lick 0.71 → 0.57 → 0.67 →
**0.74**. Positive in **9 of 9** arm-animal pairs.

**The practice confound is measured and inert.** Across the ~70-day PRE-STROKE span — same
practice, no lesion — decodability does not rise with day: mean slope **−0.009 per 30 days**.
Practice predicts a slight DECLINE over the stroke-to-chronic interval, against gains of +0.02 to
+0.20.

### `epoch_15h` — WHERE it rotates (`scripts/rest_migration/rotation_maps.py`)

Haufe patterns (`A = Cov(X)·β`) from one block-matched decoder per epoch, in the shared joint
basis, per spout position. Figures: `epoch_15h_rotation_maps_<arm>.png` (per-position rows,
epoch-delta columns) and `epoch_15h_gain_vs_rotation.png` (the gain × rotation plane).

Cosine between the pre-stroke and epoch readout direction, cohort:

| arm | acute | subacute | chronic |
|---|---|---|---|
| lick | 0.44–0.81 | 0.67–0.86 | 0.55–0.83 |
| cue | 0.22–0.61 | 0.49–0.83 | 0.31–0.67 |
| ENL | 0.04–0.56 | 0.33–0.69 | 0.24–0.54 |
| rest | −0.02–0.18 | −0.04–0.54 | 0.03–0.46 |

**lick > cue > ENL > rest** — the execution window's readout is most preserved, rest rotates most.
The same ordering the transfer matrix gives from accuracy alone, which is two independent measures
agreeing.

### Significance — 200 draws, and what it does NOT license

`epoch_15h_rotation_regions.csv` carries two per-cell p values: `cos_p_vs_noise` (one-sided, does
the readout turn further than estimation noise) and `gain_resid_p` (two-sided, on the RESIDUAL
log-gain so the epoch-wide scale cancels). 240 cells = arm x contrast x animal x position.

| | |
|---|---|
| cosine p < 0.05, UNCORRECTED | **153 / 240 (64%)** |
| residual-gain p < 0.05 | 64 / 240 (27%) |
| surviving Bonferroni | **0** |

**THE ZERO IS NOT A RESULT — IT IS THE DRAW COUNT, AND THE FAMILY WAS WRONG TOO.**

*240 = 4 windows × 3 contrasts × 6 positions × animals-with-data* (18 acute — PS95 has one acute
session; 24 subacute — all four; 18 chronic — PS94 has none). **The ANIMAL axis does not belong in
the correction family**: animals are REPLICATES of one hypothesis, not separate hypotheses.
Correcting across them asks "does far-contra rotate acutely in cue" as four different questions.
**The honest family is 72** (window × contrast × position).

It barely helps, because the draw count binds either way — the empirical floor at 200 draws is
`1/201 = 0.00498`:

| family | n | alpha | reachable at 200 draws | draws needed |
|---|---|---|---|---|
| × animals (as first reported) | 240 | 0.00021 | no | >= 4,799 |
| **window × contrast × position** | **72** | 0.00069 | no | **>= 1,439** |
| one window | 18 | 0.00278 | no | >= 359 |
| one window, one epoch | 6 | 0.00833 | **yes** | — |

So ~1,440 draws (~2.5 h), not the ~5,000 (~8 h) first implied. **Never report "nothing survived
correction" from this run** — no cell CAN survive at 200 draws whatever the effect size.

**AND BONFERRONI IS THE WRONG CORRECTION ANYWAY — USE A PERMUTATION MAX-STATISTIC.**

The six positions within a window are NOT independent. `LogisticRegression` is multinomial, so the
class coefficients are identified only up to a constant shift across classes: the six patterns are
coupled by construction. That is the same coupling that makes MEANref unusable as a deficit
measure. Bonferroni assumes independence and is therefore conservative by an unknown amount here.

**THE FIX, and the machinery already exists in this repo.** The map families correct with
`maxstat` (`epoch_15r_*_stats.csv` carries a `correction` column reading `maxstat`). The same
construction applies to the cosines:

1. For each permutation draw, compute the cosine for **all cells in the family** from the SAME
   split-half draw.
2. Record the draw's **most extreme** statistic across the family (the max |z|, or the min cosine).
3. The family-wise null is the distribution of those per-draw maxima.
4. A cell is significant at FWE 0.05 if it beats the 95th percentile of that distribution.

This respects the coupling — correlated cells produce correlated draws, so the max distribution is
narrower than the independence assumption implies — and it needs **no more draws than the per-cell
test**, because the family-wise null is built from the draws already being run. It is strictly
better than Bonferroni here, not merely different.

**THE FAMILY IS window × contrast × position = 72, NOT 240.** Animals are REPLICATES and belong in
the replication count, not the correction. See the table above.

**IMPLEMENTATION NOTE:** `null_delta` currently returns per-position draws independently. A
max-statistic version needs the per-draw values held together across positions — the draws are
already synchronised (same `seed_ns|null{d}`, same split), so it is a restructuring of what is
returned, not extra computation.

**PER-ANIMAL REPLICATION IS THE DEFENSIBLE SUMMARY**, and it is consistent — animals with >= 3 of 6
positions significant:

| arm | acute | subacute | chronic |
|---|---|---|---|
| ENL | PS92,93,94 | PS92,93,94 | PS92,93 |
| cue | PS92,93,94 | **PS92,93,94,95** | PS92,93,95 |
| lick | PS92,93,94 | PS92,93,95 | PS92,93 |
| rest | PS92,93,94 | PS92,93,94 | PS92,93 |

**STILL MISSING: an across-animal statistic.** These are per-cell tests counted by animal, not a
cohort test. The project's standard is the nested animals->sessions bootstrap; that has not been
applied to the cosines.

### WHICH STATISTICS ARE MULTIPLE-COMPARISON CORRECTED — the split matters

| statistic | corrected? | over what |
|---|---|---|
| `15h` **map outlines** | **YES** | max-statistic FWE across the in-mask components, WITHIN a position |
| `15h` `cos_p_vs_noise` | no | per cell; uncorrected across the 72-cell family |
| `15h` `gain_resid_p` | no | same |
| `15h` bootstrap CI (`--boot`) | no | 95% per cell; over 72 cells ~3.6 exclude the truth by chance |
| `15g` cell p (block permutation) | no | per cell |
| `15g` asymmetry / ceiling gain | **no p at all, by choice** | per-animal replication counts instead |

**The MAPS are properly family-wise corrected; the SCALARS are not.** Full per-panel rationale is
in DECISIONS.md ("The statistics of every 15g / 15h panel").

### THE BRAIN MASK IS NOW IN THE STATISTICS, NOT ONLY THE DISPLAY

Measured 2026-09-17: **only 49 of 87 components (PS93) sit inside `beta_maps.stat_mask`.** The other
**44%** are olfactory bulb and glue/window edge. Before the fix they could be flagged and outlined,
AND they entered the max-statistic family — so a bulb component setting a draw's maximum raised the
threshold for every cortical component, a power loss paid by the components that matter. They are
now excluded from the family and the outline is intersected with the mask.

**Every figure rendered before this fix carries contours in the bulbs.** Re-render before use.

---

## 2. THE FRAMING QUESTION — AND WHAT 15g/15h CANNOT ANSWER

Priya, 2026-09-17, and it is the single most clarifying thing in this arm:

> *"there are two ways to approach this analysis: (1) comparing each position's response across
> epochs and (2) comparing a position's response RELATIVE TO OTHER POSITIONS' across epochs."*

**15g and 15h are entirely framing (2).** `LogisticRegression` is multinomial, so `coef_[p]` is the
direction separating position *p* from the other five; `A = Cov(X)·β_p` inherits it; the cosine
compares that CONTRAST's direction across epochs; the residual gain explicitly subtracts the
cross-position mean.

**The blind spot follows: a change affecting all six positions equally is INVISIBLE to 15g/15h.**
If the lesion halved every position's response uniformly, the discriminative directions could be
untouched — cosine at ceiling, residual gain zero — while framing (1) reported a 50% loss.

Framing (1) lives in the ENCODER and the map families. `STATUS_2026-09-12` already reasons about
this split: **MEANref couples positions** ("one losing drive hands the other five an unearned
increase") and is flagged NOT a deficit measure; **RESTref** (then called QUIETref) keeps them
independent.

### GAIN IS NOT A RESPONSE AMPLITUDE — measured, r = +0.039

`gain = ||A||_epoch / ||A||_pre`. Correlated against the evoked family's amplitude ratio over 60
(animal × position × epoch) cells: **r = +0.039**. They are unrelated, for structural reasons:

* evoked amplitude is FIRST-moment (mean ΔF/F vs a baseline); `||Cov(X)·β||` is SECOND-moment
* `β` is one-vs-rest, so gain asks how *p* differs FROM THE OTHER FIVE
* gain carries the epoch-wide `Cov(X)` scale

**Decomposition:** splitting each epoch's six gains into a common factor and a position-specific
residual gives mean |common| 0.397 against mean sd(residual) 0.356 — **the epoch-wide factor is
1.11× the position-specific spread**. So roughly half of any gain number is how loud the epoch was.
**Read gain against the other five positions in the same epoch, never against 1.0.** That is why the
gain test is on the RESIDUAL log-gain.

So three distinct quantities, each needing its own name:

| quantity | measures | lives in |
|---|---|---|
| evoked amplitude | response size to that position | `epoch_14` / `evoked_amplitude` / RESTref |
| **gain** | distinctiveness vs the other five, × epoch scale | `epoch_15h` |
| **cosine** | direction of the readout, scale removed | `epoch_15h` |

---

## 3. METHOD TRAPS FOUND AND FIXED — read before building any similar contrast

1. **NEVER COMPARE TWO `retained` RATIOS ACROSS DIRECTIONS.** They have different denominators, so
   `retained(B->A) − retained(A->B) = r·(1/ceil_A − 1/ceil_B)` is nonzero for symmetric raw data.
   Measured: raw transfer symmetric in 3/3 animals, normalised asymmetry +0.404 / +0.441 / −0.001,
   predicted +0.432 / +0.331 / +0.053 from the ceilings alone. **The first version printed
   "consistent with ADDITION" and was reporting its own denominators.** Direction contrasts are
   taken on RAW above-chance. In DECISIONS.md; generalises to any "fraction retained" comparison.
2. **THE RAW |change| MAP LOCALISES THE BASIS, NOT THE LESION.** It correlates with a null built
   from two PRE-STROKE halves at **r = +0.83 / +0.81**. Component footprint mass spans **67×**, so a
   component-space unit norm projects large midline components to large pixel mass whatever their
   coefficient — retrosplenial topped every panel, including the null. Each component is now scored
   against ITS OWN split-half null.
3. **A COSINE NEEDS A NOISE CEILING, AND THE CEILING NEEDS MATCHING TOO.** Absent, 0.64 could be a
   large rotation or a noisy estimate of none. A PRE-ONLY ceiling was then systematically
   pessimistic — its models saw 5-6 sessions against the epoch models' 11 — and PS92's ENL ceiling
   came out at +0.260 while its cosines were 0.410-0.437, i.e. ABOVE their own ceiling. Now each
   epoch's own reliability is measured and the cosine is attenuation-corrected in both:
   `cos / sqrt(rel_pre · rel_epoch)`.
4. **AVERAGING OVER POSITIONS HID THE RESULT.** The cue arm's far-contra cosine of −0.042 sat among
   five values near +0.6. Everything is per position now, in the canonical Near Ipsi → Far Contra
   order (`CONF_LABELS`; `POSITION_NAMES` puts close_CENTER at 0, so sorting by code transposes the
   figure against every neighbour).
5. **THE MAP AND THE COSINE ARE DIFFERENT QUANTITIES.** The map is scale-FREE (each component
   against its own noise); the cosine is scale-WEIGHTED. PS93 cue acute: far_center has mean |z|
   **11.1** with 20% of its change in the top-weight components and a cosine of 0.37, while far_R
   has mean |z| 3.7, 37% in the top weights, and a cosine of **−0.29**. **Judge rotation by the
   bars, not by map brightness.**
6. **COLOUR SEMANTICS ARE RESERVED.** `tab:blue/red/orange/green` ALREADY mean PS92/93/94/95 —
   that is `configs/animals.yaml`, not a local choice. Windows take brown/teal/olive; epochs
   delegate to `epoch_figures.EPOCH_GREY`, which already owned the grey ramp.
7. **MASK THE MAPS.** `beta_maps.stat_mask` removes olfactory bulbs and the glue/window edge, where
   `U` is smallest and the Allen warp least constrained.

---

## 4. PENDING

### Science
* **~~RE-MEASURE THE REST-BASELINE DRIFT~~ — mostly DONE, see §0b.** `uniform_shift_check` puts the
  restdock05 baseline shift at a median **3.1%** of the evoked signal, against the ~52% the QUIET-era
  number implied. `_RESTref_` IS usable for framing (1). What remains is narrow: that script measured
  sensitivity to a DEFINITIONAL change (the lick buffer), not the ACROSS-EPOCH drift. The epoch-wise
  version is cheap and would close the question outright.
* **BUILD framing (1) WITH ANATOMICAL RESOLUTION — THREE SEPARATE REFERENCE FAMILIES, BY ROI.**
  Priya, 2026-09-17: *"we could look at `raw` (corrected SVD) cue maps, precue-normalized cue
  ('increment') maps, and rest-normalized cue maps"* — three analyses, not one with options.

  | family | asks | its OWN failure mode |
  |---|---|---|
  | **raw** (corrected SVT) | absolute post-cue activity | cross-day MULTIPLICATIVE scaling (expression, bleaching, window) |
  | **precue-normalised** (increment) | how much the cue ADDS | violates F12 — the pre-cue window carries real anticipatory position signal (LOSO 0.510), so it subtracts real code |
  | **rest-normalised** | activity above the resting state | rest COMPOSITION shifts post-stroke (delta-rest 2.56x chronically); the LEVEL is stable to ~3.1% |

  **The point is that the three failure modes are different**, so agreement across them is a real
  test — the same logic the repo already applies: *"Two independent references, sharing no
  subtrahend, give the SAME ordering."* A result in only one family is a property of that
  subtrahend.

  **RAW IS MORE MEANINGFUL THAN AN EARLIER NOTE IN THIS FILE SUGGESTED.** The objection "you cannot
  plot raw because the signal is already high-passed" is a ZEROPHASE-era objection. Under
  `meegkit_hpfit` the drift fit EXCLUDES strobe−0.25 s → cue+4 s, so the fit never sees the trial
  window; the polynomial is subtracted everywhere but the evoked response survives relative to it.
  Raw is the closest thing to absolute this pipeline has. Rationale: the encoder answers framing (1) but
  cannot say WHERE; 15g/15h say where but are framing (2). Notes for whoever builds it:
  - `REFERENCES = ("mean","rest","restw","precue")` has no unreferenced member, but `parts["raw"]`
    already exists upstream — adding `"raw"` is a few lines.
  - **"No normalisation" is not available**: the signal is already high-passed, so a raw map is
    implicitly referenced to its own surround. What raw DOES avoid is a subtrahend that moves with
    epoch. What NOTHING here fixes is cross-day MULTIPLICATIVE scaling (expression, bleaching,
    window) — subtracting a baseline does not touch a gain term. `crossday_intensity` owns that.
  - **ROI beats pixels here**: the current correction is `maxstat` over **2,022 bins**; Allen ROIs
    would be a few dozen tests, and it sidesteps the rim problem the `edge_enrichment` /
    `suppressed` columns exist for. Infrastructure exists — the deck already runs decode in an
    Allen-ROI basis and `Basis.regions` carries a per-component label.
  - **Area-normalise the ROI aggregation.** Footprint mass spans 67× and regions hold differing
    component counts; a naive sum reproduces the retrosplenial artefact in ROI clothing.
  - PRECUEref's documented objection stands (it violates F12 — the pre-cue window carries real
    anticipatory signal, LOSO 0.510 — so it measures the cue-evoked INCREMENT). But its subtrahend
    is per-trial and therefore cannot drift across epochs, which is exactly the weakness at issue.
    Put it BESIDE RESTref, not instead of it.
  **HOW TO BUILD IT FROM COLD CONTEXT:**

  1. `wfield_local/position_reference_maps.py` — `REFERENCES = ("mean", "rest", "restw", "precue")`.
     Add `"raw"`. `session_raw_maps` ALREADY returns `parts["raw"]`, so `reference_maps` needs one
     branch: `if reference == "raw": return parts["raw"]`. Nothing else computes it.
  2. The three families are then `raw`, `precue`, `restw` (use **restw**, the position-WEIGHTED
     flat baseline — `rest` is time-local and couples the comparison to trial timing).
  3. Render `cue_working` and `lick_lick` for each. `maps_by_epoch(align, variant)` already
     produces `{animal: {epoch: {position: {label: {reference: map}}}}}`, so adding the reference
     gets the family for free.
  4. **AGGREGATE BY ROI, and normalise.** `joint_locanmf.Basis.regions` gives a per-component Allen
     label; `Basis.A` gives footprint mass. **Footprint mass spans 67x across components and
     regions hold differing component counts**, so aggregate as a MEAN per ROI (or area-weighted),
     never a sum — a naive sum reproduces the retrosplenial artefact in ROI clothing (§3.2).
  5. Correct with the permutation max-statistic over ROIs (§ above), not Bonferroni.
  6. **Mask first** with `beta_maps.stat_mask` — olfactory bulbs and the glue/window edge.

  **WHAT WOULD MAKE THE RESULT BELIEVABLE:** agreement across all three references. Each has a
  DIFFERENT failure mode, so a regional effect present in all three is not a property of any one
  subtrahend. A result in only one is.

  **WHAT NONE OF THEM FIXES:** cross-day MULTIPLICATIVE scaling (expression, bleaching, window
  clarity). Subtracting a baseline does not touch a gain term. `crossday_intensity` owns that and
  should be checked before amplitudes are compared across epochs.

* **EXTEND THE MAX-STATISTIC ACROSS CELLS.** The construction already used within a position
  (`significant_components`) generalises to the 72-cell family: hold each draw's statistic across
  ALL cells, take the per-draw maximum, threshold at its 95th percentile. It needs no more draws
  than are already run and is strictly better than Bonferroni here, because the six positions are
  coupled (multinomial coefficients are identified only up to a constant shift across classes).
  `null_delta` returns per-position draws independently today; the draws are ALREADY synchronised
  (same `seed_ns|null{d}`, same split), so this is a restructuring of what is returned, not extra
  computation.

* **ACROSS-ANIMAL STATISTIC + MULTIPLE-COMPARISON CORRECTION.** `cos_p_vs_noise` and
  `gain_resid_p` are PER CELL. There is no cohort test and no correction across 6 positions × 3
  epochs × 4 arms = 216 cells. With three animals carrying chronic data, per-animal replication
  counts are the more defensible summary.
* **SUBACUTE** — resolved as a binning artefact (STATUS_2026-09-16 §6); the reporting rule is in
  DECISIONS.md.
* **PS94 has no chronic epoch** because it has not plateaued — fails on DRIFT, not residual. One
  more session at ≥ 0.996 of baseline sets `chronic_from = 25`; the DATE is irrelevant.
* DLC tongue tracking; identify the substitute code directly rather than by frozen/refit divergence.

### Housekeeping
* Fix `PREPROCESSING_DECISION.md`'s stale "nothing has been re-run" line (§0a).
* Finish the quiet→rest rename in ~29 slide strings.
* Cross-check 15g's top row against the published `5rm` numbers — not yet done.
* `coding_rtdrift.png` is 08-22 and placed but not in the manifest figures map.
* `epoch_14_beta_maps_MEANref_lick_lick_stats` Far Contra / acute−pre: refused BY DESIGN (20-trial
  floor); the deck note now carries a `|| text` fallback.
* Settling transient; sklearn `multi_class` removed in 1.8; roadmap #3 fold-in of the
  `scripts/rest_migration/` builders into `wfield_local`.
* **Machine split:** dev here, nightlies on the other box. It must be on current `main` before it
  runs any rest analysis, or it writes pre-correction numbers under the same filenames.

---

## 4b. HOW TO PICK THESE UP COLD — commands, outputs, and the order they depend on

```bash
export PYTHONPATH=.
# 15g -- the transfer matrix (4 arms, ~4 min; rest is disk-cached after the first run)
python -m scripts.rest_migration.transfer_arms --arms ENL cue lick rest --perm 200

# 15h -- rotation maps. --boot is OPTIONAL and roughly doubles runtime (refits per draw).
python -m scripts.rest_migration.rotation_maps --arms ENL cue lick rest --null-draws 200 --boot 200
```

**Runtimes measured 2026-09-17:** 15g ~190 s all four arms. 15h ~1,880 s at `--null-draws 200`
(the nightly uses 12, ~180 s). `--boot 200` adds roughly the same again.

**Outputs, all in `<labcams>/grant_figures/epoch/`:**

| file | what |
|---|---|
| `epoch_15g_transfer_<arm>_sessions.csv` | one row per (train, test, session) cell |
| `epoch_15g_transfer_<arm>_matrix.csv` | the aggregate, incl. `retained_vs_own_ceiling` |
| `epoch_15g_transfer_by_window.png` | matrices + diagonal + symmetry + ceiling gain |
| `epoch_15h_rotation_regions.csv` | per (arm, animal, contrast, position, region): cosine, both p values, reliabilities, gain, CI, n_sessions |
| `epoch_15h_rotation_maps_<arm>.png` | per-position z maps with FWE outlines + cosine bars |
| `epoch_15h_gain_vs_rotation.png` | the gain x rotation plane |

**The rest arm's stem carries the VARIANT** (`epoch_15g_transfer_restdock05_*`), not `rest`, so two
rest definitions can never overwrite each other. The task arms are named by window.

**KEY FUNCTIONS, and what each guards:**

* `wfield_local/transfer_matrix.py` — shared core for BOTH arms. `retained` (never compare two
  across directions), `block_permute`, `_matched` (whole blocks), `haufe_pattern`, `fold_bins`,
  and the reserved colour palettes (`arm_color`, `epoch_color` -> `epoch_figures.EPOCH_GREY`).
* `scripts/rest_migration/transfer_arms.py` — `collect_task` (via `_pooled_bundle` +
  `_class_select`), `collect_rest` (disk-cached).
* `scripts/rest_migration/rotation_maps.py` — `epoch_patterns` (+ per-epoch reliability),
  `null_delta` (returns |delta| draws, cosine draws, residual-gain draws), `in_mask_components`,
  `significant_components` (max-stat FWE), `bootstrap_cosine_ci`, `component_outline`.

**GUARDS:** `tests/test_transfer_matrix.py` (18) pins the normalisation trap in closed form, the
positive/negative controls, the training-size match and the block null. Run before touching any of
it.

## 5. NIGHTLY

`nightly_figs` runs, beside 15f/15s:

```
scripts.rest_migration.transfer_arms  --arms ENL cue lick rest --perm 200      (~4 min)
scripts.rest_migration.rotation_maps  --arms ENL cue lick rest --null-draws 12 (~3 min)
```

The rest arm now reads a **disk cache** (`session_cache`, keyed on basis id + bins + gate + rest
variant). It was the only arm paying full collection every run — ~5 min per animal against ~40 s
for a task arm — which made an 11-minute arm gate a figure the other three already supported.

---

# ADDENDUM — 2026-09-17 afternoon. READ THIS BEFORE TRUSTING ANY 15h/15k ARTEFACT ON THE SHARE.

Everything above this line predates the afternoon session. Four things changed that invalidate
earlier artefacts, and one pitfall cost most of the day.

> **READ §A10 BEFORE QUOTING ANY NUMBER FROM §A6-§A9.** The component mask gate was measuring the
> wrong thing all day — it ran on `stat_mask`, which is `brain_mask` eroded by 16 px for a
> pixel-contour purpose — so every `15h` and `15k` result in those sections is on a **30-region**
> vocabulary that is missing `SSp-m_left`, ipsilesional mouth cortex. The adopted gate is two
> criteria (`brain_mask >= 0.85` AND `stat_mask >= 0.50`) and gives **33 regions**. The directions
> and shapes are unlikely to move; every number is provisional until the re-run lands.

## A0. THE PITFALL THAT COST THE DAY — A STALE IMPORT

**Published `epoch_15h` contours were unmasked, and every check I ran said they were fine.** The
mask fix was commit `fc02bf4` at **13:51**; the run that drew the figures started at **13:39** and
had already bound the module. It ran pre-fix code for its whole life and wrote filenames
indistinguishable from correct ones.

**THE DIAGNOSTIC MISTAKE MATTERS MORE THAN THE BUG.** Asked whether outlines leaked, I tested
`component_outline` **in the working tree**, got 0 px outside the mask, and produced a confident
WRONG explanation (a white-on-white colormap artefact). *Testing the code on disk answers a
different question from what a running process did.*

**WHAT SETTLED IT — use this, it takes a minute:** crop the panel out of the PNG by detecting its
axes frame, resize `beta_maps.stat_mask` onto it, overlay the true edge. The map filled the mask
exactly; the contours did not.

**AND IT WAS NOT COSMETIC:** the same process wrote `epoch_15h_rotation_regions.csv`, so bulb/glue
components were in the max-statistic family and those FWE thresholds are inflated.

**RULE, now in DECISIONS.md:** a figure written by a process that started before the last relevant
commit is UNTRUSTED until verified against the artefact. Date the process against `git log`.

Guards added: git SHA stamped into the 15h cache **with a `-dirty` suffix**; contours re-intersected
at DRAW time with a printed leak count; `in_mask_components` LOUD on a grid mismatch (it silently
returned all-True, which removes the mask from the statistics with no error).

## A1. `epoch_15h` HAS A CACHE — `--replot` IS 5 s, NOT 30 MIN

`save_cache` / `load_cache` / `rebuild`. Stores COMPONENT-space `z` and `sig` (~90 floats/cell), not
pixel renders, and `--replot` re-projects through the same `to_pixels` / `component_outline` as the
live path. Refuses if no cache exists rather than silently recomputing.

```bash
python -m scripts.rest_migration.rotation_maps --replot          # redraw only
```

**KNOWN WEAKNESS, NOT YET FIXED:** `save_cache` is called AFTER the whole arm loop (line ~665), so a
crash at 2 h loses everything including the cache. **Move it inside the arm loop** so each completed
arm persists. Do this first — it is small and it protects every later run.

## A2. NEW: `epoch_15j` — THE REST BASELINE *DOES* DRIFT ACROSS EPOCHS

`scripts/rest_migration/rest_baseline_epoch_drift.py`. This is the narrow question §0b left open, and
it QUALIFIES §0b's conclusion.

**6 of 11 animal-epoch cells significant, 3 of 4 animals. Shifts 7–43% of the evoked signal (median
~0.15)** — ~5x `uniform_shift_check`'s 3.1% and far below the retired QUIET-era ~52%. All three are
different questions.

| animal | acute | subacute | chronic |
|---|---|---|---|
| PS92 | 0.139 | **0.279*** | **0.342*** |
| PS93 | **0.148*** | **0.126*** | **0.166*** |
| PS94 | 0.165 | **0.432*** | (no chronic) |
| PS95 | 0.144 | 0.074 | 0.074 |

**UNITS:** dimensionless — `‖mean_E(b/k) − mean_pre(b/k)‖`, each session's baseline divided by its
OWN evoked norm `k` (which is what cancels cross-day MULTIPLICATIVE scaling). It is a whole-map
vector norm, and it is an **UPPER BOUND on the amplitude bias**, attained only if the shift is
aligned with the evoked pattern; orthogonal shifts cost ~r²/2 (0.15 → ~1%). So 0.15 means "between
~1% and ~15%".

**Acute is the LEAST affected epoch** despite carrying the largest deficit — so this does not simply
track the lesion. PS95 is null throughout.

**THE COSINE CARRIES NO p AND MUST NOT BE READ AGAINST ZERO.** `d` holds `−mean_pre(bn)` and
`e_ref = mean_pre(post/k) − mean_pre(bn)` holds the same vector, same sign → biased positive before
any drift exists. Two nulls were tried; the algebra defeats both (a subset of pre CANCELS the term:
4 of 20 gives `d = 0.8(mean_ga − mean_gb)`). A pre-drawn null is ANTI-CONSERVATIVE. Ships as
`cos_with_evoked_biased`. Fixing it needs `e_ref` from HELD-OUT pre sessions; 12 pre vs 6 acute does
not leave room. **Open.**

## A3. NEW: `epoch_15k` — THE THREE REFERENCE FAMILIES, BY `Basis.regions`

`scripts/rest_migration/reference_family_roi.py`. Families `raw` / `precue` / `restw`, chosen
because their failure modes DIFFER (see the module docstring). An effect in all three is not a
property of any subtrahend; an effect in one names the subtrahend to suspect.

**`raw` IS NOW A REAL REFERENCE.** Three things had to land together — the `reference_maps` branch,
`REFERENCES`, and `epoch_grant_figures._REF_TEXT` — because a missing caption raises PER-ARM after
earlier figures are written, so the render **exits 0 having silently produced fewer figures**. That
has happened twice (`restw`, `precue`). `tests/test_reference_registry.py` (16 tests) now makes the
registry self-enforcing.

**AGGREGATION IS BY COMPONENT, NOT PIXEL** (Priya: *"let's abandon ROI for the basis.regions"*).
Component value = footprint-weighted mean `sum(|A_c|·map)/sum(|A_c|)`; region = MEAN over its
components, never a sum. Pixel-ROI swamped focal changes — MOs is 15,613 px. Vocabulary is the
INTERSECTION of Allen labels across animals (components themselves do not correspond: 95/87/90/95).

**`MIN_IN_MASK_FRAC` RAISED 0.5 → 0.75** (`rotation_maps.py`). A half-outside component cast a full
vote on half the evidence. Cost ~12% of components (55/49/52/48 → 48/43/45/44); region vocabulary
34 → **30**. This affects **BOTH 15h AND 15k**.

**THE COHORT STATISTIC IS A CI, NEVER A p.** `cohort_delta` resamples ANIMALS → SESSIONS, forming
the delta inside each draw from independently resampled pre and epoch sessions. With 4 animals an
animal-level permutation has 2⁴ = 16 assignments → floor 0.0625, so **no cell can reach p < 0.05 at
any effect size** (same trap as the 200-draw Bonferroni, via cohort size).

**PER-SESSION VECTORS ARE SAVED** (`epoch_15k_region_vectors_<align>.npz`) so any later statistic is
a re-read, not another `maps_by_epoch` pass.

## A4. SUPERSEDED — DO NOT QUOTE

* **The first 15k cue table** (acute 16 / subacute 3 / chronic 8 "all three families") was
  PIXEL-weighted AND used an **ANY-ANIMAL** rule — a cell counted if any one animal cleared p<0.05,
  giving a per-cell false-positive rate of ~1−0.95⁴ = **18%**. Different weighting AND different
  criterion: a different answer, not a rescaling.
* **All 15h artefacts written before ~16:00** — unmasked contours and inflated FWE thresholds.
* `above_chance` **meant two different things** in two CSVs in one directory (`acc − null` in 15g,
  `(acc−chance)/(1−chance)` in 15f). Renamed `acc_minus_null` / `above_chance_frac`; 15g regenerated
  byte-identical.

## A5. STATE AT HANDOFF, AND WHAT TO RUN FIRST

**In flight at the time of writing:**
* `rotation_maps --arms ENL cue lick rest --null-draws 200 --boot 200` — ran at
  `MIN_IN_MASK_FRAC = 0.5` and **completed**. Its 264-cell result is §A6. A 0.75 repeat was launched
  17:24 as `--min-in-mask-frac 0.75 --tag _mf075`; **the 0.5 outputs are KEPT for comparison**, not
  overwritten, and either set redraws from its own cache in 5 s.
* `reference_family_roi --align cue --perm 2000` — **DID pick up 0.75**, and the first draft of this
  section said it did not. Its console header reports **30 regions**, which is the 0.75 vocabulary;
  0.5 gives 34. So only `15h` needed the re-run, not both.

**AND THAT IS THE INVERSE OF A0's STALE IMPORT — A LAZY ONE.** `shared_regions` imports
`in_mask_components` **inside the function**, and that call lands after `maps_by_epoch`, ~20 min into
the run, which was after the edit. A module-level import binds at process START; a function-level
import binds at FIRST CALL. So one long-running process can be half-old code and half-new, and
neither the start time nor the file mtime tells you which half. **The rule from A0 is the fix for
both: date the ARTEFACT, not the process.** Here the artefact was the region count in the header
(30 ≠ 34) — the same kind of measurement as the PNG forensics in A0, and it took ten seconds.

**ORDER TO PICK UP** — items 1, 3 and 4 were DONE the same evening; see §A7 for what they
found and §A7's own pickup list, which supersedes this one:
1. ~~Move `save_cache` INSIDE the arm loop (A1).~~ DONE — and the SHA now comes from STARTUP.
2. Re-run 15h; verify with the cache-based mask check — rebuild outlines and assert 0 px outside
   `stat_mask`. **Do not verify by eye and do not verify against the working tree.**
3. ~~Re-run `15k` for `--align lick` at 0.75.~~ DONE — 30 regions, 1665 session vectors.
4. ~~`15k` has **no figure**.~~ DONE — `reference_family_figure`, both arms. §A7.
5. Cross-check: do `15k`'s all-three regions sit under `15r`'s outlined blobs? Two different test
   units and correction families agreeing is much stronger than either alone. Disagreement is
   diagnostic — see the dilution rule in the `15h`/`15r`/`15k` contrast in DECISIONS.md.

**COMMANDS**

```bash
export PYTHONPATH=.
python -m scripts.rest_migration.rotation_maps --arms ENL cue lick rest --null-draws 200 --boot 200
python -m scripts.rest_migration.rotation_maps --replot
python -m scripts.rest_migration.reference_family_roi --align cue  --perm 2000
python -m scripts.rest_migration.reference_family_roi --align lick --perm 2000
python -m scripts.rest_migration.rest_baseline_epoch_drift --perm 2000
```

**OUTPUTS** (all `<labcams>/grant_figures/epoch/`): `epoch_15h_rotation_cache*.npz`,
`epoch_15j_rest_baseline_epoch_drift.csv`, `epoch_15k_reference_family_roi_<align>.csv`,
`epoch_15k_cohort_<align>.csv`, `epoch_15k_region_vectors_<align>.npz`.

**UNCOMMITTED AT HANDOFF:** the 15k rewrite (component aggregation, cohort, vector save), the `raw`
reference + registry tests, `MIN_IN_MASK_FRAC`, and the DECISIONS entries. Last commit `bc6d055`.
Nothing pushed since.

**STILL OPEN FROM BEFORE:** the max-statistic across the 72-cell 15h family; the held-out-pre fix
for the 15j cosine; `15k` figure; quiet→rest rename (~31 strings); 15g vs published 5rm cross-check.

## A6. THE ROTATION-vs-GAIN RESULT, and a per-cell claim I got wrong

Measured on the completed 2026-09-17 run (`epoch_15h_rotation_regions.csv`, 264 unique
arm x contrast x animal x position cells). **These are `MIN_IN_MASK_FRAC = 0.5` numbers**; the 0.75
comparison is the `_mf075` set.

### The conclusion: the code ROTATES while getting STRONGER, and the two are independent

| arm | acute | subacute | chronic |
|---|---|---|---|
| ENL | cos +0.33 / ceil +0.52, gain 1.04 | +0.39 / +0.52, gain 0.95 | +0.36 / +0.41, gain **1.32** |
| cue | +0.39 / +0.81, gain 1.09 | +0.65 / +0.81, gain 1.11 | +0.48 / +0.78, gain **1.64** |
| lick | +0.69 / +0.83, gain 1.05 | +0.72 / +0.83, gain 1.03 | +0.63 / +0.80, gain **1.28** |
| rest | +0.12 / +0.47, gain **1.73** | +0.22 / +0.47, gain **2.67** | +0.22 / +0.48, gain **1.57** |

1. **Every PANEL MEAN sits below its noise ceiling**, in all 12 arm x epoch combinations, so the
   readout direction genuinely turns. Ordering is the familiar one — lick most preserved, then cue,
   then ENL, then rest (0.12 against a 0.47 ceiling, nearly total).
2. **Gain is >= 1 almost everywhere and RISES INTO CHRONIC.** The code is not fading; it becomes
   MORE distinctive while pointing elsewhere. Same fact `15g` reports from accuracy alone (every
   window ends above its pre-stroke ceiling), reached through an unrelated quantity.
3. **The two axes are near-orthogonal: r = -0.142** between cosine and log-gain over 264 cells, and
   all four quadrants are populated (rotated+strengthened 78, preserved+strengthened 78,
   preserved+weakened 67, rotated+weakened 41). If rotation and weakening were one phenomenon the
   diagonal quadrants would be empty. They are not.

So "did the lesion degrade the code or move it" answers **MOVED, AND AMPLIFIED** — most in rest,
least in lick.

### THE CORRECTION: 24% OF CELLS SIT ABOVE THEIR OWN CEILING

I wrote "every cosine sits below its noise ceiling". **That is a PANEL-MEAN claim stated per cell,
and it is false per cell.** Priya caught it from the figure, where the ENL and cue chronic panels
plainly carry points at ~+0.85-0.9 above their ceiling lines.

**64 of 264 cells (24%) are above their own ceiling**, and evenly spread — 3-8 per panel, no arm or
epoch concentrating them:

| arm | acute | subacute | chronic |
|---|---|---|---|
| ENL | 5/24 | 8/24 | 7/18 |
| cue | 3/24 | 5/24 | 4/18 |
| lick | 7/24 | 3/24 | 4/18 |
| rest | 6/24 | 6/24 | 6/18 |

**THE FLATNESS IS THE INTERPRETATION.** A cosine above its ceiling means the epoch pattern matches
pre BETTER than two halves of pre match each other, which is only possible by chance — the ceiling
is the upper bound on what is measurable. ~24% at a ceiling built from split-half reliability is
about what noise in both estimates predicts. Concentrated in one arm it would mean the ceiling was
mis-estimated there; spread evenly it simply marks the resolution limit.

**PRACTICAL RULE: a cell at or above its ceiling carries NO rotation claim in either direction.** It
is not "preserved", it is UNMEASURABLE. The rotation result rests on the cells well below the
ceiling — which is why `cos_attenuation_corrected` and `cos_p_vs_noise` are in the CSV and the raw
cosine alone is not the statistic.

### AND GAIN >= 1 DOES NOT MEAN BIGGER RESPONSES

Repeated here because the plane invites the misreading. Gain is `||A||_epoch / ||A||_pre` on the
DECODER pattern: second-moment, one-vs-rest, and carrying the epoch-wide `Cov(X)` scale. Correlated
against evoked amplitude at **r = +0.039** over 60 cells, and roughly HALF of any gain number is how
loud the epoch was (common factor 1.11x the position-specific spread). It means MORE DISTINCTIVE
FROM THE OTHER FIVE POSITIONS, not "responded more" — which is why the test runs on residual
log-gain and why the deficit must be read from the REST-referenced maps and the encoder instead.

## A7. `epoch_15k` HAS A FIGURE, AND IT SAYS THE ACUTE CHANGE IS BARELY ANATOMICAL

`scripts/rest_migration/reference_family_figure.py`. Reads `epoch_15k_cohort_<align>.csv` and
draws; REFUSES if the table is absent rather than recomputing, exactly as `15h --replot` does. Both
arms rendered at `MIN_IN_MASK_FRAC = 0.75`, 30 regions:

```
epoch_15k_reference_families_cue.png
epoch_15k_reference_families_lick.png
```

Rows = epoch, columns = the three references + an agreement panel. Dot = the cohort CI excludes
zero; ring = all three references agree. **Colour scales differ BETWEEN columns on purpose** —
`raw` has no subtrahend, `precue` is an increment, `restw` is activity above rest, and only epochs
WITHIN a column are comparable.

### THE RESULT: ASK WHETHER "WHERE" HAS AN ANSWER BEFORE READING THE REGION LIST

A table of significant regions reads as a localisation even when the same shift hits every region.
So `global_vs_regional` takes the mean over regions at each position — a change common to the whole
cortex — and reports how much of the 30 x 6 table it accounts for. Printed on every draw:

| align | | acute | subacute | chronic |
|---|---|---|---|---|
| **cue** | raw / precue / restw | **75 / 76 / 69 %** | 51 / 58 / 39 % | 46 / 42 / **26 %** |
| **lick** | raw / precue / restw | **31 / 11 / 22 %** | 21 / 26 / 14 % | 47 / 47 / 27 % |

**THE CUE ARM'S ACUTE PANEL IS THE LOUDEST AND THE LEAST ANATOMICAL.** Three quarters of it is a
near-UP / far-DOWN shift that hits every region alike — 100% of the 30 regions share the sign at
near-ipsi, near-contra, far-middle and far-contra, in all three references. Quoting its significant
regions as a locus would be reading a global change through an anatomical vocabulary. By chronic
the global part falls to 26% (restw) and the change becomes genuinely regional.

**THE LICK ARM IS THE OPPOSITE AND IS ANATOMICAL FROM THE START** — 11-31% global at acute,
concentrated in orofacial somatosensory cortex.

**THE SIGN FLIP KILLS THE `raw` FAMILY'S ONE REAL CONFOUND.** Cross-day multiplicative scaling —
the confound `raw` cannot remove and no subtraction removes either — pushes all six positions the
SAME direction. Near up while far goes down is not available to it.

**THE SAME THREE AREAS TOP EVERY CHRONIC LIST, IN BOTH ARMS AND ALL THREE REFERENCES:**
`SSp-m_right`, `SSp-un_right`, `SSp-n_right`. Acute (cue) departs elsewhere: `SSp-n_left`,
`SSp-un_left`, `SSp-ul_left`.

### AND THE HEMISPHERE SUFFIX NOW MEANS SOMETHING, BECAUSE IT WAS MEASURED

Every lesion in the cohort is LEFT-sided, so `_left` vs `_right` decides whether the line above
reads as IPSILESIONAL-acute -> CONTRALESIONAL-chronic or the reverse. **`locanmf_analysis_deck`
said, in a comment AND on the slide, that a LEFT lesion makes the LEFT hemisphere the
CONTRALESIONAL one** — the exact inversion its own next sentence warned about. Fixed.

**THE ADVICE ATTACHED TO IT WAS RIGHT** ("read the left panel"), which is why it survived: the
figure never looked wrong. Only the stated reason was, and a wrong reason travels to places the
advice does not.

**VERIFIED, NOT ARGUED** — an image-space flip in the Allen warp would have made the corrected word
wrong again. `reference_family_figure --hemisphere-check` reads the saved vectors and tests the one
thing that cannot be flipped: somatosensation is crossed, so a RIGHT-side spout must drive the LEFT
cortex harder. Pre-stroke, `restw`, **11 of 11 areas match** — every `_left` positive, every
`_right` negative (SSp-n +0.34/-0.35, MOp +0.23/-0.14, SSp-un +0.18/-0.17, SSp-bfd, SSp-ul). The
spout half was already fixed by `anatomical_labels` calling close_R/far_R 'contra'.

So: **LEFT = ipsilesional, and also the hemisphere representing the impaired RIGHT side.** The
acute cue departures are ipsilesional; the chronic ones, in both arms, are contralesional.

### TWO LIMITS THAT MUST TRAVEL WITH THAT SENTENCE

* **`SSp-m` HAS NO LEFT PARTNER IN THE VOCABULARY.** The 30 shared regions include `SSp-m_right`
  and not `SSp-m_left` — no in-mask component carried that label in all four animals. The
  strongest chronic region therefore has nothing on the other side to be contrasted against, and
  "right mouth cortex" is not by itself evidence of a laterality.
* **THE LICK ARM IS CONDITIONED ON TRIALS THE ANIMAL LICKED.** Acute far-contra has NO cohort cell
  in ANY animal, because there were no such trials — the figure labels that column rather than
  leaving it as anonymous grey. Every other lick cell is the map of the attempts that HAPPENED, a
  selection that changes across epochs. This is the same attempt/engagement confound that inverted
  `epoch_14`'s MEANref amplitudes.

### GUARDS ADDED THIS EVENING

* `rotation_maps` **checkpoints after every arm** (cache + CSV). A four-arm run is ~3 h and both
  were written only at the end, so a crash in arm 4 discarded arms 1-3 AND the cache that exists to
  make a redraw free.
* The git SHA is now captured at **STARTUP** and passed into `save_cache`. It used to be read when
  the cache was written, hours later — so `568d935`, a commit made DURING the run, was stamped onto
  results it had no part in. The code that actually ran was `bc6d055`.
* `reference_family_roi` prints `MIN_IN_MASK_FRAC` and the SHA in its header. Counting regions
  (30 vs 34) was the only way to tell the two thresholds apart, which is how A5 went wrong.

### ORDER TO PICK UP FROM HERE (supersedes A5's list)

1. **When `_mf075` lands** (`rotation_maps ... --min-in-mask-frac 0.75 --tag _mf075`, arm 2/4 at
   18:29, ~21:00 expected): compare against the 0.5 set in A6. Both are on the share and BOTH
   redraw from their own cache in 5 s, so this is a diff, not a re-run. **The running process
   predates the per-arm checkpoint** — it still writes only at the end.
2. **Cross-check `15k` against `15r`**: do the all-three-families regions sit under `15r`'s
   outlined blobs? Different test units and different correction families agreeing is much stronger
   than either alone; disagreement is diagnostic — see the dilution rule in the `15h`/`15r`/`15k`
   contrast in DECISIONS.md. Do this on the LICK arm first: it is the anatomical one (11-31%
   global), so a localisation there is a real claim, whereas the cue acute panel is mostly a
   global shift and would agree with anything.
3. **Decide what to do about the global component itself.** It is 75% of the cue acute effect and
   currently nothing tests it. It is a per-position, all-region change — which is framing (1)'s
   territory and invisible to 15g/15h by construction (they are relative to the other positions).
   A one-line statistic on the position means would say whether it is significant, and it is
   probably the largest single effect in the arm.
4. **Max-statistic across the 72-cell `15h` family** (still open from before).
5. **Held-out-pre fix for the `15j` cosine** (still open; 12 pre vs 6 acute does not leave room).

**COMMANDS ADDED THIS EVENING**

```bash
python -m scripts.rest_migration.reference_family_figure --align cue
python -m scripts.rest_migration.reference_family_figure --align lick
python -m scripts.rest_migration.reference_family_figure --align lick --hemisphere-check
```

**STILL OPEN FROM BEFORE:** quiet->rest rename (~31 strings); `15g` vs published `5rm` cross-check;
the sklearn `multi_class` one-liner in `rest_position_decode.py:81`; the `coding_rtdrift` manifest
gap; DLC tongue tracking.

**NOTHING HAS BEEN PUSHED SINCE `fc02bf4`.** `bc6d055`, `568d935`, `bdadb12` and this evening's
work are local to the `rest-baseline` worktree.

## A8. THE GLOBAL SHIFT IS REAL, AND CUE vs LICK SPLIT THE DEFICIT IN TWO

`reference_family_figure --global-test`. A7 measured that ~75% of the cue arm's acute change is a
per-position shift common to every region, then left it untested — every `15k` statistic is per
REGION, so the largest component of the effect got none of the inference. This is that test: the
unit is ONE SESSION'S UNWEIGHTED MEAN over the 30 regions, estimated with the project's nested
animals -> sessions bootstrap, reported as an interval.

### CUE (every trial, responded or not): far-contra COLLAPSES acutely and RECOVERS by chronic

| epoch | near ipsi | far middle | far contra |
|---|---|---|---|
| acute | **+0.0067** [+0.0016, +0.0115] 3/4 | **-0.0093** [-0.0172, -0.0012] 4/4 | **-0.0102** [-0.0121, -0.0081] 4/4 |
| subacute | +0.0046 n.s. | -0.0012 n.s. | -0.0029 n.s. |
| chronic | **+0.0091** [+0.0045, +0.0142] 3/3 | +0.0053 n.s. | +0.0015 n.s. |

`raw` shown; `precue` and `restw` agree cell for cell on every starred cell, and acute far-contra
is 4/4 animals in ALL THREE families with the tightest interval in the table. 14 of 54 cells clear
of zero.

**THE SHAPE IS THE ENCODER'S** (0.749 -> 0.286 -> 0.745) and `15g`'s "every window ends above its
pre-stroke ceiling", reached from a third, unrelated quantity: an all-region amplitude mean.

### LICK (only trials the animal actually licked): acute goes UP, and far-contra DOES NOT EXIST

12 of 51 cells clear of zero and **every one of them is POSITIVE**. Acute near-ipsi +0.0077 4/4,
near-contra +0.0050, far-ipsi +0.0054 4/4; `precue` adds near-middle and holds into subacute.
**Acute far-contra has no cell at all, in any animal or family — there were no such trials.**

### READ THE TWO TOGETHER — THIS IS THE ATTEMPT/RESPONSE DISSOCIATION MADE EXPLICIT

Same cortex, same window length, same basis, same estimator; the arms differ only in which trials
contribute. Cue takes them all and sees far-contra drive collapse. Lick takes only the attempts
that happened and sees, if anything, a LARGER response.

**So the acute deficit lives in WHETHER THE ATTEMPT HAPPENS, not in the cortical response given
that it did.** That is the same confound that inverted `epoch_14`'s MEANref amplitudes
(far-contra 0.47 -> 2.08 once engaged-only was fixed) — except here it is measured on both sides
of the split instead of contaminating one number.

**IT DOES NOT SETTLE WHICH DIRECTION THE CAUSALITY RUNS.** A cortex that fails to represent the
far-contra target and an animal that does not attempt it produce the same two tables. What the
pair rules out is the simplest reading of the cue panel on its own — "far-contra cortical drive is
lost" — because on the trials where the movement occurs the drive is present and larger.

### WHY THIS NEEDED ITS OWN TEST AND IS NOT IN `15g`/`15h`

Framing (2) scores a position RELATIVE to the other five, so a shift raising all six equally is
invisible to it by construction. This shift is NOT uniform across positions -- it reverses sign
between near and far in the cue arm -- so the two framings should agree here. **That agreement is
worth checking rather than assuming**, and it is the natural companion to the `15k`/`15r`
cross-check already on the list.

```bash
python -m scripts.rest_migration.reference_family_figure --align cue  --global-test
python -m scripts.rest_migration.reference_family_figure --align lick --global-test
```

## A9. THE AGREEMENT PANEL IS SIGNED, AND THE AGREED SET IS ON A BRAIN

Both asked for by Priya, 2026-09-17: *"can that be color-coded red shades or blue shades for
increase or decrease? Can we make a version of the agree in all 3 that is on the Allen CCF
picture?"*

**SIGNED AGREEMENT** (`signed_agreement`, last column of `epoch_15k_reference_families_*.png`).
Blue = decrease, red = increase, darker = more references agreeing, -3..+3. A count alone said
three references agreed that SOMETHING changed without saying which way, and the direction is the
whole content of the cell. The cue acute panel now reads at a glance: a red block on the near
columns, a blue block on far-middle and far-contra.

**A CELL WHOSE SIGNIFICANT REFERENCES DISAGREE IN SIGN IS NOT AVERAGED.** It goes to 0 and gets an
x, because two references pointing opposite ways is a fact ABOUT THE REFERENCES and averaging would
hide exactly what the three-family design exists to surface.

**MEASURED: ZERO SUCH CELLS.** Both arms, all three epochs, 0 sign disagreements. Where two or
three references both clear zero they always agree on direction — which is a real (and not
guaranteed) strengthening of the whole design, and it is now printed on every draw so a future run
that breaks it cannot do so quietly.

**THE CCF VERSION** (`epoch_15k_agreed_ccf_<align>.png`). Rows = epoch, columns = spout position,
painted only where all three references clear zero; colour is the `restw` value, Allen edges
overlaid, the analysed mask dashed.

* **Cue acute far-contra: 25 of 30 regions painted, all blue, bilateral.** That is the global shift
  of A7/A8 seen as anatomy — a whole-cortex loss, not a locus.
* **Lick: every painted region red, in every epoch**, and acute far-contra labelled NO TRIALS IN
  ANY ANIMAL rather than "0 regions". Two different facts must not share a label.
* **Chronic, both arms: the focal dark-red parcel is in the RIGHT hemisphere** — contralesional,
  and the one 15k keeps naming (`SSp-m_right`, `SSp-un_right`, `SSp-n_right`).

**THE FLAT COLOUR INSIDE A REGION IS THE RESULT, NOT A RENDERING SHORTCUT** — the unit is a LocaNMF
component mean spread over its Allen footprint, and the sharp edges are the parcellation's. Read
beside `15r`, whose blobs are pixel-level and whose edges ARE data. That is the cross-check still
outstanding.

## A10. THE COMPONENT GATE WAS ASKING A PIXEL-CONTOUR QUESTION — EVERY 30-REGION RESULT IS SUPERSEDED

**THE ADOPTED GATE, from 2026-09-17 evening, is TWO criteria and both must hold:**

```
mass in brain_mask >= 0.85    IS THIS CORTEX          (bulbs + painted glue already out of it)
mass in stat_mask  >= 0.50    IS ENOUGH OF IT AWAY FROM THE WINDOW RIM TO BE TESTABLE
```

Everything on the share tagged `_erodedgate`, and everything `15h`/`15k` written before this,
used the SUPERSEDED single criterion `mass in stat_mask >= 0.75`. It gives **30 regions**; the
adopted gate gives **33**.

### THE BUG: `stat_mask` IS `brain_mask` ERODED BY 16 px, AND THAT EROSION IS FOR SOMETHING ELSE

`beta_maps.stat_mask` erodes 16 px for ONE purpose — keeping PIXEL-LEVEL contours out of the
window rim, where edge enrichment was measured at ~2x. Applied to a COMPONENT it asks a question
it was never built to answer, and it **penalises a region for being LATERAL rather than for being
untrustworthy**.

**WHAT IT COST, and it is not hypothetical.** PS93's `SSp-m_left` components are **97.5% inside
`brain_mask`** — 2.5% genuinely off-brain — but **27-29% of their mass lies in that rim**, so they
scored 0.688/0.705 and failed at 0.75. The `15k` vocabulary is an INTERSECTION over animals, so one
animal's window removed `SSp-m_left` **for all four**: ipsilesional mouth cortex, in a cohort whose
deficit is orofacial, and the missing partner for the `SSp-m_right` that tops every chronic list.

Systematic, not PS93-specific: the `SSp-m` ATLAS REGION loses **30% of its pixels** to the erosion,
and every `SSp-m` component in every animal loses 17-29% of its mass. Priya asked the question that
found it — *"how is > 75% masked?? erosion criteria?"*

### BUT RELAXING TO `brain_mask` ALONE IS ALSO WRONG

It admits 8 regions, of which **three are admissible and five are not, for two distinct reasons**
(Priya, same session: *"I think FRP, RSP, VIS are probably not real"*, *"VIS right in particular is
tough to interpret bc the glue covers VISl"* — right on both counts):

| region | mass in brain | mass in stat | mass under glue | verdict |
|---|---|---|---|---|
| **SSp-m_left** | 0.975-0.989 | 0.688-0.808 | 0.011-0.026 | **ADMIT** — lost purely to erosion |
| RSPagl_left | 0.923-0.957 | 0.621-0.865 | 0.043-0.077 | admit |
| VISal_right | 0.961-0.987 | 0.623-0.682 | 0.013-0.039 | admit |
| PL_left/right | 0.891-1.000 | **0.410-0.679** | 0 | REJECT — half rim |
| FRP_left/right | 0.798-1.000 | **0.000-0.165** | 0 | REJECT — *entirely* rim |
| VISp_right | **0.758-0.847** | 0.541-0.701 | **0.153-0.242** | REJECT — the glue |

**FRP's ENTIRE 389 px ATLAS FOOTPRINT IS INSIDE THE RIM** — 0 px survive in `stat_mask`. That is not
a region the old gate treated unfairly; it is a region this window cannot test at any threshold.
**VISp_right loses 31.3% of its footprint to the painted glue**, and because the glue is ALREADY
subtracted from `brain_mask`, occlusion shows up as MISSING MASS — which is why it cannot reach
0.85. Two failure modes, two criteria, each stating what is actually wrong rather than a number
tuned until the list looked right.

### AND THE THRESHOLD QUESTION DISSOLVES

**33 regions at 0.80, 0.85 AND 0.90 alike.** The earlier sensitivity (34 at 0.5, 30 at 0.75) was
the eroded gate slicing through lateral cortex, not a real degree of freedom. **This retires the
"run it at 0.5 as well" plan rather than answering it** — the comparison worth keeping is
eroded-gate vs corrected-gate, and both sets are now on the share.

Whether RSP and VIS cells then SURVIVE is for the three-family agreement to decide, not for the
gate. In the eroded-gate cue results the RSP rows are almost all "only raw", which is that filter
working.

### THE EROSION ITSELF STAYS WHERE IT BELONGS

16 px for PIXEL contours, unchanged. The repo already knows it does not finish the job there either
— `EDGE_ENRICHMENT_MAX = 2.0` exists because *"a single erosion radius cannot be tuned to cover
every panel without eating real cortex"*, and near-middle chronic is still 44% rim-occupied after
erosion. **IF THE RADIUS IS EVER RE-DERIVED, MEASURE PER ANNULUS, not cumulatively.** Deriving
annuli from the recorded cumulative table suggests near-middle's enrichment PEAKS at 16-32 px
(~2.6x) — i.e. just INSIDE where the erosion draws its boundary — but that is arithmetic on an old
table, not a measurement, and must not be acted on before it is one.

### HOW THE OLD SET IS PRESERVED

23 files COPIED (never moved, never deleted) to `*_erodedgate.*`, size-verified on write. The
corrected run then writes the UNTAGGED names, so **canonical means correct**. Tagging the NEW set
instead would leave the superseded one wearing the canonical name — the trap this repo keeps
meeting.

### WHAT HAS TO BE RE-RUN

`15h` (all four arms) and `15k` (cue, lick, precue). Everything in §A6-A9 was computed on the
superseded gate: the rotation/gain table, the global-vs-regional decomposition, the global shift
test, the CCF figures and every region list. **THE DIRECTIONS AND THE SHAPES ARE UNLIKELY TO MOVE**
— the gate adds three regions and removes none — but every NUMBER is provisional until re-run, and
no region list from before this is complete.

### DONE IN A SEPARATE WORKTREE, ON PURPOSE

`.claude/worktrees/mask-gate` (Priya: *"can you do these changes in a worktree to avoid affecting
the current run?"*). Two runs were in flight and `reference_family_roi` imports `in_mask_components`
**lazily**, so an edit in `rest-baseline` would have been picked up MID-RUN by a process that
started before it. The change was reverted there, and the precue run's own header then confirmed
it: `MIN_IN_MASK_FRAC = 0.75   git ed429e7`, 30 regions — the same gate as its cue and lick
siblings. **Verified from the artefact, not assumed from the clock** (§A0, §A5).

## A11. THE PRE-CUE ARM, AND HOW INDEPENDENT THE THREE REFERENCES ACTUALLY ARE

### `15k --align precue` RUNS ON TWO FAMILIES, AND THE MISSING ONE IS THE POINT

With `align=precue` the feature window is `[cue - 2 s, cue]` and the `precue` BASELINE is its own
final second, so that reference would **subtract the window from itself** (Priya, 2026-09-17:
*"obviously for precue we should not do a precue reference"*). `configs/defaults.yaml` has said so
since 09-13 and `epoch_grant_figures` already skipped the combination — but `session_raw_maps`
computed it UNCONDITIONALLY, so any other caller would have got a perfectly plausible map built
from a self-referential subtraction. **Guarded at the source now**, and dropped loudly again in
`reference_family_roi`, because a silently two-family run would still have printed an "ALL THREE
families" table with three meaning two.

Every family count is now DERIVED from the data (`families_in`). `_ccf_figure` kept a literal
`n_families=3` for one run and painted NOTHING for the precue arm while the console line above it
reported 13 cells agreeing in all 2 — a figure contradicting its own summary, with only the figure
visible.

### THE RESULT: THE PRE-CUE WINDOW INVERTS THE CUE WINDOW AT FAR-CONTRA

Eroded-gate numbers, provisional pending the re-run. Acute far-contra: **7 regions, all INCREASES**
— against a near-total DECREASE at far-contra in the cue window (§A8). Acute near-middle goes the
other way (5 regions, decreases). Chronic is near-middle and far-middle decreasing, 6 regions each,
far-contra empty. Global fraction 64% acute, 77% / 54% chronic.

### AND A CAVEAT THAT MUST TRAVEL WITH THAT ARM

Removing the `precue` family leaves `raw` and `restw` — **which are the pair correlated at
r = +0.954**. Measured over the 540 shared cue cells:

| | r between deltas | Jaccard of the significance CALLS |
|---|---|---|
| raw vs restw | **+0.954** | 0.530 |
| raw vs precue | +0.875 | 0.386 |
| precue vs restw | +0.860 | 0.473 |

(lick: +0.940 / +0.743 / +0.777, Jaccard 0.539 / 0.368 / 0.454)

So in the pre-cue arm **"both references agree" is close to "one says so"**, and the agreement gate
that does real work in the cue and lick arms barely filters here. The counts show it: 62/51/53 cells
clear in ALL 2 for precue, against 16/1/7 in ALL 3 for cue. **Read the pre-cue panels as one
reference with a consistency check, not as two independent ones.**

### THE SAME MEASUREMENT DEFENDS THE THREE-FAMILY DESIGN ELSEWHERE

The rest subtraction moves a cue cell by rms 0.0019 against a signal rms of 0.0057, so `raw` and
`restw` are near-duplicates as ESTIMATES. But their CALLS agree on only 37-54% pairwise, and 54
cells still clear zero in all three at acute — so the gate discards half to two thirds of what each
family flags and is not a tautology. **Both halves belong in the record: the deltas are correlated,
the calls are not.**

**ZERO SIGN DISAGREEMENTS** in the cue and lick arms, all epochs — where two or three references
both clear zero they always agree on direction. One cell disagrees in the precue arm at acute. The
count prints on every draw so a future run cannot break this quietly, and a disagreeing cell is
never averaged into a direction it does not have: it goes to 0 and gets an x.

## A12. THE ROTATION ARM IS ON THE DECK — it was rendered for a week and referenced nowhere

**`epoch_15g`, `15h`, `15j` and `15k` appeared in NO deck list.** Grepping
`locanmf_analysis_deck.py` for any of them returned nothing; `_EPOCH` stopped at `15f`/`15r`/`15s`/
`15x`. Twelve figures from a week's work existed only as files on the share.

**THE DECK'S COMPLETENESS CHECK CANNOT CATCH THIS** — it reports figures it EXPECTS and is silent
about ones it was never told about. Same failure as `locanmf_rsa`'s hemisphere panels, which ran
every night and were read by nobody until they were placed, and the same reason section H exists.

**13 slides now in section I**, in narrative order: `15g` (does the code still read the brain) →
`15h` ×4 arms + the gain-vs-rotation plane (where and how it turns) → `15j` (is the subtrahend
itself moving) → `15k` ×3 arms, matrix + CCF brain each (where it changes, anatomically).

**EXPLICIT FILENAMES, NOT GLOBS, and that is load-bearing.** `epoch_15h_rotation_maps_*.png` would
also match the `_mf075` and `_erodedgate` variants kept for comparison, and the deck would show
three versions of every arm with nothing on the slide saying which gate produced which.

**`epoch_15j` HAD NO FIGURE AT ALL** — it wrote a CSV and a console table, which is a family the
deck cannot place and the completeness check cannot miss. It now draws one, and `--replot` rebuilds
it from the CSV in seconds rather than repaying the permutation pass.

**THREE SHARED LEGEND TEMPLATES** (`_ROT_LEGEND`, `_REF_LEGEND`, `_CCF_LEGEND`) because the four
`15h` arm slides and the three `15k` arm slides differ by one sentence each. Four hand-copied
300-word legends is how two of them end up describing different methods.

**THE LEGENDS QUOTE NO GATE-DEPENDENT CELL COUNTS.** They state the method, the gate, the
qualitative result and the name of the CSV that holds the numbers. Every figure in this arm was
about to be regenerated on the corrected gate when they were written, and a legend carrying a
number that the next run changes is the staleness this repo keeps paying for.

### CORRECTED-GATE RE-RUN — what has landed

| | regions | acute / subacute / chronic agreeing in all families |
|---|---|---|
| `15k` cue | **33** (was 30) | 60 / 3 / 24 (was 54 / 3 / 21) |
| `15k` lick | **33** | 49 / 9 / 17 (was 43 / 8 / 13) |
| `15k` precue | running | (was 13 / 4 / 12, two families) |
| `15h` all four arms | running | — |

**ZERO SIGN DISAGREEMENTS** in cue and lick, all epochs, on the corrected gate as on the old one.
Directions and shapes held exactly as predicted: the gate adds three regions and removes none, and
the extra agreement is those regions' worth of evidence.
